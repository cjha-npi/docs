// file: doxy-plus.js

// #region ⚠️ WARNING: DO NOT CREATE CUSTOM HASH/HREF
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
//
// TL;DR: In some edge cases, a custom hash can make the default
// side nav disappear.
//
// Doxygen uses NAVTREEINDEX to resolve a hash/href. NAVTREEINDEX is
// split across multiple files and stores entries as: root + hash
// (where "root" is the page URL ending with ".html").
//
// For a given hash/href, Doxygen looks up the root + hash combo in
// NAVTREEINDEX. If it cannot find that combo, it falls back to selecting
// just the root. This works fine with Doxygen's own hashes, but with a
// custom hash it can sometimes fail to find the root as well: while
// searching, it may advance to another NAVTREEINDEX file (because the
// index is split) that does not contain the root entry. In that case,
// Doxygen falls back to index.html, i.e. the "Main Project" item.
//
// This fallback is a serious problem for us because unlike the default
// sidenav, the sidenav generated by doxy-plus in this file does not have
// index.html as a single top level item. So, the fallback to index.html
// can give an incorrect sense of which page we currently are.
//
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// #endregion ⚠️ WARNING: DO NOT CREATE CUSTOM HASH/HREF

// #region 🟩 STORE.JS INFO
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
//
// We are using the store.js file `store.everything.min.js`. It lets us store
// an expiry time with each key. The expiry is checked only when the key is
// accessed. If the current time is past the expiry time, store.js removes the
// stored data and returns `undefined`.
//
// NOTE: store.js namespaces can only contain letters, digits, underscores, and dashes.
//
// To set the expiry time, we pass a third parameter to the `set()` function
// provided by store.js. This third parameter is the current time plus the time
// to live in milliseconds. For example:
// `store.set(key, val, Date.now() + 1000)`
//
// This stores `val` under `key` for 1000 milliseconds from now. The key is not
// removed immediately when it expires. It is removed only the next time it is
// accessed after the expiry time has passed.
//
// How the data is stored:
// In localStorage, store.js stores the same logical key using two separate
// keys when using a namespace with expiry support. For example, a key named
// `html_pages` may be stored as:
// `__storejs_F-Doxy-Main_html_pages`
// `__storejs___storejs_F-Doxy-Main_expire_mixin_html_pages`
//
// Using this knowledge, we can iterate through all keys in localStorage and
// extract the namespace name and key name from the second key above. The first
// key format does not give us a clear separator between the namespace name and
// the key name, but the second key does. Its format is:
// `__storejs___storejs_<namespace name>_expire_mixin_<key name>`
//
// So, when iterating through localStorage, we only inspect keys that start
// with `__storejs___storejs_` and then check whether they contain the
// separator `_expire_mixin_`. If both conditions are true, we can extract the
// namespace name and key name.
//
// This lets us find all keys related to any project, as long as they are under
// the same domain, or all local files if no URL is involved. If we want to
// remove an expired key, we simply access it through its namespace name and
// key name. If it has expired, store.js removes it automatically.
//
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// #endregion 🟥 STORE.JS INFO

// #region 🟩 DATE-TIME INFO
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

// For a long time I was using window.DOXY_PLUS_DATE_TIME as a true source
// of date-time for when the Doxygen output was generated. This date-time
// was inserted by me at the very end of footer.html like this:
//
// <!-- Storing Generation DateTime for use in doxy-plus.js -->
// <script> window.DOXY_PLUS_DATE_TIME = '$datetime'; </script>
//
// <!-- Doxy-Plus Custom JavaScript File -->
// <script type="text/javascript" src="$relpath^doxy-plus.js"></script>
//
// </body>
// </html>
//
// This provided me with custom text like `Thu Apr 16 2026 16:35:06` in this 
// (doxy-plus.js) file and I compared it with stored value to check if it was
// a new generation of data. However, the problem with this issue was that
// the window.DOXY_PLUS_DATE_TIME was computed for each html page as it was
// generated by Doxygen, so one page would get `Thu Apr 16 2026 16:35:05` and
// the other would get `Thu Apr 16 2026 16:35:06` because it was generated one
// second later. This created issues because the two texts did not match, resulting
// in my script discarding stored data and regenerating it for no reason.
// 
// The solution:
// I found out that even though different html pages had different values for
// window.DOXY_PLUS_DATE_TIME, the footer always shows the exact same value on
// each page. This turns out to be coming from doxygen.css file. In doxygen.css
// file at the very end there is a block of code:
//
// html {
// --timestamp: 'Thu Apr 16 2026 16:57:38'
// }
// span.timestamp { content: ' '; }
// span.timestamp:before { content: var(--timestamp); }
//
// Here the 'span.timestamp:before' is what is shown on the footer as the date-time.
// The "--timestamp: 'Thu Apr 16 2026 16:57:38'" is assigned during the Doxygen
// generation and since all html pages share the same doxygen.css file, all of it
// gets the same date-time value.
//
// So, instead of using my window.DOXY_PLUS_DATE_TIME as the the source to know
// wether the generation is a newly build Doxygen output or is an old one, I can
// use 'getComputedStyle(document.documentElement).getPropertyValue('--timestamp')'
// which gives me the above doxygen.css '--timestamp' value (which is a string value).

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// #endregion 🟥 DATE-TIME INFO

// #region 🟩 CONSOLE.TABLE LIMITATION
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

// In Chrome DevTools Console, the console.table() command has a major restriction.
// A single cell can display maximum of 100 characters of text, anything more will
// lead to text being truncated in the middle. For example a text like this:
// 'This is an example text to show how the console.table truncates the text which is longer than 100 characters'
// will appear like this:
// 'This is an example text to show how the console.ta…ates the text which is longer than 100 characters'
// Because of this reason almost all of the console outputs have been moved to non console.table() format.

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// #endregion 🟥 CONSOLE.TABLE LIMITATION

; (function ($) {
  'use strict';

  // #region 🟩 BROKEN HASH PATCH
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  // Patch jQuery to not throw when "#id" fragments contain CSS-invalid characters such as '/'.
  (function () {
    // Purpose
    //   Patch jQuery so that simple "#id" selectors created from Doxygen hash fragments do not throw.
    //   Doxygen (notably navtree.js) sometimes does $(hash) where hash comes from a link fragment.
    //   With some settings (e.g. INLINE_SIMPLE_STRUCTS=YES in doxyfile), those fragments can contain 
    //   '/' and other characters that are not valid in a raw CSS id selector. jQuery/Sizzle then throws:
    //   "Syntax error, unrecognized expression: #d5/d42/..."
    //   The exception aborts the click handler and can break navigation/highlighting/page-nav logic.
    //
    // What this patch changes
    //   It monkey-patches jQuery's internal constructor $.fn.init (used by $()/jQuery()).
    //   For selectors that are a very simple "#something" (not complex CSS expressions), it escapes
    //   the id portion so that characters like '/' become '\/'. This makes the selector valid CSS,
    //   so jQuery returns an empty set instead of throwing.
    //
    // Scope and safety
    //   The patch is intentionally narrow:
    //   - Only touches selector strings that look like a simple "#id" (no spaces, combinators,
    //     attribute selectors, pseudo selectors, selector lists, etc.).
    //   - Does not change complex selectors to avoid altering legitimate query semantics.
    //   - Uses jQuery.escapeSelector when available (jQuery 3+), otherwise uses a conservative
    //     fallback escaping rule.
    //
    // Load order
    //   This code installs immediately if jQuery is already present. If not, it retries briefly
    //   using setInterval until jQuery loads, then patches once and stops retrying.
    //
    // Behavioral impact
    //   - Eliminates the "unrecognized expression" console errors for invalid hash-based selectors.
    //   - Prevents exceptions inside Doxygen handlers, allowing navigation logic to continue.
    //   - If the escaped id does not exist in the DOM, jQuery returns an empty set (normal behavior).

    function escapeIdSelector(id) {
      // Prefer jQuery's official escaper when present (jQuery 3+).
      if (window.jQuery && typeof window.jQuery.escapeSelector === "function") {
        return window.jQuery.escapeSelector(id);
      }

      // Fallback: escape anything that is not [A-Za-z0-9_-]
      return String(id).replace(/[^a-zA-Z0-9_-]/g, function (ch) { return "\\" + ch; });
    }

    function patchOnce() {
      var $ = window.jQuery;
      if (!$ || !$.fn || !$.fn.init) return false;
      if ($.fn.init.__doxyPlusPatched) return true;

      var origInit = $.fn.init;

      function isSimpleIdSelector(s) {
        // Only rewrite very simple "#something" selectors.
        // Do NOT touch complex selectors (spaces, combinators, attrs, pseudos, commas).
        return typeof s === "string" &&
          s.length >= 2 &&
          s.charAt(0) === "#" &&
          s.indexOf(" ") === -1 &&
          s.indexOf(">") === -1 &&
          s.indexOf("+") === -1 &&
          s.indexOf("~") === -1 &&
          s.indexOf(",") === -1 &&
          s.indexOf("[") === -1 &&
          s.indexOf(":") === -1;
      }

      var patchedInit = function (selector, context, root) {
        if (isSimpleIdSelector(selector)) {
          var rawId = selector.slice(1);
          selector = "#" + escapeIdSelector(rawId);
        }
        return origInit.call(this, selector, context, root);
      };

      patchedInit.prototype = origInit.prototype;
      patchedInit.__doxyPlusPatched = true;
      $.fn.init = patchedInit;

      return true;
    }

    // Install ASAP, even if our file loads before jquery.js.
    if (patchOnce()) return;
    var t = setInterval(function () { if (patchOnce()) clearInterval(t); }, 10);
  })();

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // #endregion 🟥 BROKEN HASH PATCH

  // #region 🟩 HELPERS
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  // Displays a fatal error message on the webpage and in console.
  function dispFatalError(msg) {
    const root = document.body || document.documentElement;
    const pre = document.createElement('pre');
    pre.textContent = `FATAL ERROR:: ${msg}`;
    root.replaceChildren(pre);
    throw new Error(msg);
  }

  // Deep-clone a nested array structure that contains only arrays and primitive values.
  function deepCloneArray(item) {
    if (!Array.isArray(item)) return item;
    const out = [];
    for (const node of item) out.push(deepCloneArray(node));
    return out;
  }

  // Replaces all occurrences of "anonymous_namespace{...}" with "<...>"
  function prettierAnon(text) {
    if (typeof text !== 'string') return text;
    return text.replace(/anonymous_namespace\{([^}]+)\}/g, '<$1>');
  }

  // Compares two strings and provides highest priority to alphabets.
  function compareAlphaBeforePunctuation(a, b) {
    // Provided for sorting anonymous namespace after normal normal ones.
    // Anonymous namespace is often replaced using 'prettierAnon()' function
    // above. Then the sorting happens and so normal namespace that starts
    // with alphabets will be sorted above anonymous namespaces. For example
    // 'some_ns' will be above '<anon_ns>'.

    if (typeof a !== 'string' || typeof b !== 'string') return 0;

    const aLen = a.length;
    const bLen = b.length;
    const minLen = Math.min(aLen, bLen);

    // Compare each character till the length of the shorter string
    for (let ii = 0; ii < minLen; ++ii) {
      const aCh = a[ii].toUpperCase();
      const bCh = b[ii].toUpperCase();

      const aIsAlpha = (aCh >= 'A' && aCh <= 'Z');
      const bIsAlpha = (bCh >= 'A' && bCh <= 'Z');

      if (aIsAlpha && !bIsAlpha) return -1;
      if (!aIsAlpha && bIsAlpha) return 1;

      // If both characters are alphabets then use native compare
      const cmpResult = aCh.localeCompare(bCh, undefined, { sensitivity: 'base' });
      if (cmpResult !== 0) return cmpResult;
    }

    // In case of one string is a sub-string of another, e.g. a = 'Some' and
    // b = 'SomeFunc', then sort shorter string before longer string.
    return aLen - bLen;
  }

  // Recursively freezes the given value and everything nested inside it.
  function deepFreeze(value) {
    // Why this is needed:
    // Object.freeze(...) is shallow. It only freezes the current object/array itself,
    // not the nested objects or arrays stored inside it. So if an object contains
    // another object or array, those inner values would still remain mutable unless
    // they are also frozen explicitly.
    //
    // What this function does:
    // 1. Checks whether the passed value is a non-null object/array.
    // 2. Skips values that are primitives (string, number, boolean, null, etc.).
    // 3. Skips values that are already frozen to avoid useless repeated work.
    // 4. Freezes the current object/array itself.
    // 5. Recursively walks through all nested children and freezes them too.
    //
    // For arrays:
    // It iterates through each item and recursively freezes that item.
    //
    // For plain objects:
    // It gets all own property names using Object.keys(...) and recursively freezes
    // the value stored in each property.
    //
    // Result:
    // After this function completes, the full reachable object/array tree is frozen,
    // not just the top-level container.
    //
    // Important:
    // - This only works for values reachable from the passed root.
    // - This is runtime protection, not compile-time enforcement.
    // - In strict mode, illegal mutations throw errors. Without strict mode, some
    //   mutation attempts may fail silently.
    if (value && typeof value === 'object' && !Object.isFrozen(value)) {
      // Freeze the current object/array first.
      Object.freeze(value);

      if (Array.isArray(value)) {
        // For arrays, recurse into each element.
        for (const item of value) {
          deepFreeze(item);
        }
      } else {
        // For objects, Object.keys(value) returns an array of this object's own
        // property names, e.g. ['label', 'scope', 'value'].
        for (const key of Object.keys(value)) {
          // value[key] gets the actual value stored in that property, and that
          // nested value must also be frozen if it is an object/array.
          deepFreeze(value[key]);
        }
      }
    }

    return value;
  }

  // Waits for the first DOM element that matches selector.
  function waitFor(selector) {
    // Returns a Promise that:
    // 1. resolves with the matching element as soon as it is found,
    // 2. resolves with null if the timeout is reached first,
    // 3. rejects if selector is not a valid querySelector() string.
    //
    // Behavior:
    // 1. Check immediately in case the element already exists.
    // 2. If not found, watch DOM changes and check again after each mutation.
    // 3. Stop watching as soon as the element is found or the timeout expires.

    return new Promise((resolve, reject) => {
      let timer;
      let obs;

      // Function for cleaning up
      function cleanup() {
        if (obs) obs.disconnect();
        clearTimeout(timer);
      }

      // Function to find the selector
      function findNow() {
        try {
          const el = document.querySelector(selector);
          if (!el) return null;
          cleanup();
          //console.log(`waitFor(): Found selector "${selector}"`, el);
          resolve(el);
          return el;
        } catch (err) {
          cleanup();
          console.error(`waitFor(): invalid selector "${selector}"`, err);
          reject(err);
          return null;
        }
      }

      // Check once before starting the observer.
      if (findNow()) return;

      // Create a mutation observer that calls findNow on mutations
      obs = new MutationObserver(() => { findNow(); });

      // Observe a root node that exists even if body is not available yet.
      obs.observe(document.documentElement || document, { childList: true, subtree: true });

      // Stop waiting after the timeout.
      timer = setTimeout(() => {
        cleanup();
        console.warn(`waitFor(): Timed out waiting for selector "${selector}" after 2000 ms`);
        resolve(null);
      }, 2000);
    });
  }

  // Format a date-time value as "HH:MM:SS DD/MM/YYYY".
  function formatDateTime(dateTimeValue) {

    // Accepted dateTimeValue:
    // - Date() object:
    //   - new Date()
    //   - new Date('2026-04-23T14:30:45')
    // - date-time string:
    //   - '2026-04-23T14:30:45'
    //   - '2026-04-23'
    //   - 'April 23, 2026 14:30:45'
    //   - '2026-04-23T14:30:45Z'
    // - timestamp in milliseconds:
    //   - 0
    //   - Date.now()
    //   - 1776941445000

    const date = new Date(dateTimeValue);

    const year = String(date.getFullYear());
    const month = String(date.getMonth() + 1).padStart(2, '0');
    const day = String(date.getDate()).padStart(2, '0');

    const hours = String(date.getHours()).padStart(2, '0');
    const minutes = String(date.getMinutes()).padStart(2, '0');
    const seconds = String(date.getSeconds()).padStart(2, '0');

    return `${hours}:${minutes}:${seconds} ${day}/${month}/${year}`;
  }

  // Checks generated html or hash data and reports all errors or hard-fails.
  function checkTreeData(data, kind = 'html', hardFail = false) {

    // This function is a strict validator for checking generated tree data.
    // It accepts only html and hash kinds, verifies that the top-level data 
    // is an array, and then walks the entire nested tree recursively. For 
    // every node, it checks that the node itself is an array of exactly three 
    // entries, that node[0] is a non-empty non-whitespace string, that node[1] 
    // follows the expected rule for the selected kind, and that node[2] is 
    // either a non-empty string or a non-empty child array. While traversing, 
    // it builds a readable path for each node so error messages point to the 
    // exact failing location in the tree. All discovered problems are collected 
    // into a single errors list. If hardFail is true, the collected error text 
    // is passed to dispFatalError, which aborts execution. Otherwise, all errors 
    // are printed to the console and the function returns false. If no error is 
    // found, it returns true.

    // Rules for each node:
    // - node[0]: A non-empty string without trailing whitespaces.
    // - node[1]: A null or non-empty string without trailing whitespaces.
    // - node[2]: A non-empty array or non-empty string without trailing whitespaces. If node[2] is a string:
    //   - kind === 'html': it must end with '.html' (case-insensitive).
    //   - kind === 'hash': it must start with '#' and contain text after '#'.

    function typStr(obj) {
      if (obj === null) return 'null';
      else if (obj === undefined) return 'undefined';
      return typeof obj;
    }

    function valStr(obj) {
      if (obj === null) return 'null';
      if (obj === undefined) return 'undefined';

      try {
        const json = JSON.stringify(obj);
        return json === undefined ? String(obj) : json;
      }
      catch {
        return String(obj);
      }
    }

    const isHtml = kind === 'html';
    const isHash = kind === 'hash';

    if (!isHtml && !isHash) {
      const errTxt = `checkTreeData() - Invalid kind parameter: ${valStr(kind)}; expected 'html' or 'hash'.`;
      if (hardFail) dispFatalError(errTxt);
      else console.error(errTxt);
      return false;
    }

    const dataName = isHtml ? 'checkTreeData() - HTML Data Array Errors' : 'checkTreeData() - Hash Data Array Errors';

    const errors = [];
    errors.push(dataName);

    if (!Array.isArray(data)) {
      errors.push(`Invalid data parameter: '${typStr(data)}'; expected an array.`);
      if (hardFail) dispFatalError(errors.join('\n - '));
      else console.error(errors.join(' > '));
      return false;
    }

    const sep = isHtml ? '::' : ' ';

    function checkBranch(branch, parent = '') {
      if (branch.length === 0) return;
      let idx = 0;
      for (const node of branch) {
        let nodeName = `${++idx}`;
        let fullName = parent ? `${parent} > ${nodeName}` : nodeName;

        if (!Array.isArray(node)) {
          errors.push(`${fullName}: Node has invalid type '${typStr(node)}'.`);
          continue;
        }

        if (node.length !== 3) {
          errors.push(`${fullName}: Node length is not 3, it is ${node.length}.`);
          continue;
        }

        let name = '';
        if (typeof node[0] === 'string') {
          const trimmed = node[0].trim();
          if (trimmed.length === 0) {
            errors.push(`${fullName}: node[0] is empty or whitespace-only string.`);
          }
          else {
            if (node[0] !== trimmed) {
              errors.push(`${fullName}: node[0] text ${JSON.stringify(node[0])} has leading/trailing whitespace.`);
            }
            name = trimmed;
          }
        }
        else {
          errors.push(`${fullName}: node[0] is not a 'string'; type = ${typStr(node[0])}, value = ${valStr(node[0])}.`);
        }

        if (name.length > 0) {
          nodeName = `${idx}. ${name}`;
          fullName = parent ? `${parent} > ${nodeName}` : nodeName;
        }

        if (typeof node[1] === 'string') {
          let prefix = '';

          const trimmed = node[1].trim();
          if (trimmed.length === 0) {
            errors.push(`${fullName}: node[1] is empty or whitespace-only string.`);
          }
          else {
            if (node[1] !== trimmed) {
              errors.push(`${fullName}: node[1] text ${JSON.stringify(node[1])} has leading/trailing whitespace.`);
            }
            prefix = trimmed;
          }

          if (prefix.length > 0) {
            if (name.length > 0) {
              nodeName = `${prefix}${sep}${name}`;
              fullName = parent ? `${parent} > ${nodeName}` : nodeName;
            }
            else {
              nodeName = `${idx}. ${prefix}${sep}-`;
              fullName = parent ? `${parent} > ${nodeName}` : nodeName;
            }
          }
          else {
            if (name.length > 0) {
              nodeName = `${idx}.  -${sep}${name}`;
              fullName = parent ? `${parent} > ${nodeName}` : nodeName;
            }
          }
        }
        else {
          if (node[1] !== null) {
            errors.push(`${fullName}: node[1] must be 'null' or 'string'; type = ${typStr(node[1])}, value = ${valStr(node[1])}.`);
          }
          else {
            if (name.length > 0) {
              nodeName = `${name}`;
              fullName = parent ? `${parent} > ${nodeName}` : nodeName;
            }
          }
        }

        let processKids = false;
        if (typeof node[2] === 'string') {
          const trimmed = node[2].trim();
          if (trimmed.length === 0) {
            errors.push(`${fullName}: node[2] is empty or whitespace-only string.`);
          }
          else {
            if (node[2] !== trimmed) {
              errors.push(`${fullName}: node[2] text ${JSON.stringify(node[2])} has leading/trailing whitespace.`);
            }

            if (isHash) {
              if (!/^#.+/.test(trimmed)) {
                errors.push(`${fullName}: node[2] text ${JSON.stringify(node[2])} must start with '#' and contain a target after it.`);
              }
            }
            else {
              if (!/\.html$/i.test(trimmed)) {
                errors.push(`${fullName}: node[2] text ${JSON.stringify(node[2])} does not end with a '.html' (case-insensitive).`);
              }
            }
          }
        }
        else if (Array.isArray(node[2])) {
          if (node[2].length === 0) {
            errors.push(`${fullName}: node[2] is an empty array.`);
          }
          else {
            processKids = true;
          }
        }
        else {
          errors.push(`${fullName}: node[2] has invalid type; type = ${typStr(node[2])}, value = ${valStr(node[2])}.`);
        }

        if (processKids) checkBranch(node[2], fullName);
      }
    }

    checkBranch(data);

    if (errors.length > 1) {
      if (hardFail) {
        const errTxt = errors.join('\n - ');
        dispFatalError(errTxt);
      }
      else {
        console.group(errors[0]);
        for (let ii = 1; ii < errors.length; ++ii) {
          console.error(errors[ii]);
        }
        console.groupEnd();
        return false;
      }
    }

    return true;
  }

  // Checks and prints a tree in hierarchical format while classifying problems as errors or warnings.
  function debugTreeData(data, kind = 'default') {

    // This function is both a validator and a visual debugger for tree data. 
    // It accepts default, html, or hash kinds, confirms that the top-level 
    // data is an array, and then recursively walks the tree while building 
    // styled console output for each node. Each printed line shows the node 
    // number, visible name, optional link text, and child count, while parent 
    // paths are shown above child groups to make nesting clear. During traversal, 
    // it checks the same core structure rules as the strict validator, but it 
    // treats some conditions as warnings rather than errors depending on the 
    // selected kind. For example, default data is treated more leniently 
    // because it originates from loaded Doxygen data rather than from the 
    // generated structures. It keeps separate counts for errors and warnings, 
    // shows them inline on each node line, and prints a final summary at the 
    // end. Its purpose is not to stop execution, but to let you inspect the 
    // tree shape and immediately see where the data is malformed, suspicious, 
    // or inconsistent.

    // Rules for each node:
    // kind === 'default'
    // - node[0]: A non-empty string without trailing whitespaces.
    // - node[1]: A null, undefined or non-empty string without trailing whitespaces.
    // - node[2]: A non-empty array or null.
    // kind === 'html' or kind === 'hash'
    // - node[0]: A non-empty string without trailing whitespaces.
    // - node[1]: A null or non-empty string without trailing whitespaces.
    // - node[2]: A non-empty array or non-empty string without trailing whitespaces. If node[2] is a string:
    //   - kind === 'html': it must end with '.html' (case-insensitive).
    //   - kind === 'hash': it must start with '#' and contain text after '#'.

    const cPar = 'color: GrayText; font-size: 90%;';
    const cTxt = '';
    const cLnk = 'color: LinkText;';
    const cKid = 'color: GrayText;';
    const cErr = 'color: red;';
    const cWrn = 'color: orange;';

    const validKinds = new Set(['default', 'html', 'hash']);

    function typStr(obj) {
      if (obj === null) return 'null';
      else if (obj === undefined) return 'undefined';
      return typeof obj;
    }

    function valStr(obj) {
      if (obj === null) return 'null';
      if (obj === undefined) return 'undefined';

      try {
        const json = JSON.stringify(obj);
        return json === undefined ? String(obj) : json;
      }
      catch {
        return String(obj);
      }
    }

    if (!validKinds.has(kind)) {
      console.log(`%cInvalid 'kind' parameter. Expected 'default', 'html', or 'hash'; got: ${valStr(kind)}.`, cErr);
      return;
    }

    if (!Array.isArray(data)) {
      console.log(`%cThe ${kind} data has invalid type '${typStr(data)}'.`, cErr);
      return;
    }

    const isDefault = kind === 'default';
    const sep = isDefault ? '' : ((kind === 'html') ? '::' : ' ');

    let idx = 0;
    let iErr = 0;
    let iWrn = 0;

    function dispBranch(branch, spaces = 0, parent = '') {
      if (branch.length === 0) return;
      const txtGap = ' '.repeat(spaces);
      for (const node of branch) {

        let sGap = '';
        let sPar = '';
        let sTxt = '';
        let sLnk = '';
        let sKid = '';
        let sErr = '';
        let sWrn = '';

        const lineNo = ++idx;
        sTxt = `${txtGap}${lineNo}.`;

        if (!Array.isArray(node)) {
          ++iErr;
          console.log('%s%c%s', sTxt, cErr, ` Node has invalid type '${typStr(node)}'.`);
          continue;
        }

        if (node.length !== 3) {
          ++iErr;
          console.log('%s%c%s', sTxt, cErr, ` Node length is not 3, it is ${node.length}.`);
          continue;
        }

        let name = '';
        let namePrefixNotAdded = true;
        if (typeof node[1] === 'string') {
          const trimmed = node[1].trim();
          if (isDefault) {
            if (trimmed.length === 0) {
              sWrn = ` node[1] is empty or whitespace-only string`;
              ++iWrn
              sLnk = ' null';
            }
            else {
              if (node[1] !== trimmed) {
                sWrn = ` node[1] text ${JSON.stringify(node[1])} has leading/trailing whitespace`;
                ++iWrn
              }
              sLnk = ` ${trimmed}`;
            }
          }
          else {
            if (trimmed.length === 0) {
              sErr = ` node[1] is empty or whitespace-only string`;
              ++iErr
            }
            else {
              if (node[1] !== trimmed) {
                sErr = ` node[1] text ${JSON.stringify(node[1])} has leading/trailing whitespace`;
                ++iErr
              }
              name = `${trimmed}${sep}`;
              sTxt += ` ${name}`;
              namePrefixNotAdded = false;
            }
          }
        }
        else {
          if (isDefault) {
            if (node[1] != null) {
              sErr = ` node[1] must be 'null', 'undefined', or 'string'; it is '${typStr(node[1])}'`;
              ++iErr
            }
          }
          else {
            if (node[1] !== null) {
              sErr = ` node[1] must be 'null' or 'string'; it is '${typStr(node[1])}'`;
              ++iErr
            }
          }
        }

        if (typeof node[0] === 'string') {
          const trimmed = node[0].trim();
          if (trimmed.length === 0) {
            if (sErr.length > 0) sErr += ';'
            sErr += ` node[0] is empty or whitespace-only string`;
            ++iErr
          }
          else {
            if (node[0] !== trimmed) {
              if (isDefault) {
                if (sWrn.length > 0) sWrn += ';'
                sWrn += ` node[0] text ${JSON.stringify(node[0])} has leading/trailing whitespace`;
                ++iWrn
              }
              else {
                if (sErr.length > 0) sErr += ';'
                sErr += ` node[0] text ${JSON.stringify(node[0])} has leading/trailing whitespace`;
                ++iErr
              }
            }
            if (namePrefixNotAdded) sTxt += ' ';
            sTxt += trimmed;
            name += trimmed;
          }
        }
        else {
          if (sErr.length > 0) sErr += ';'
          sErr += ` node[0] is not a 'string', it is '${typStr(node[0])}'`;
          ++iErr
        }

        let processKids = false;
        if (typeof node[2] === 'string') {
          if (isDefault) {
            if (sErr.length > 0) sErr += ';'
            sErr += ` node[2] is of 'string' type with text ${JSON.stringify(node[2])}.`;
            ++iErr
          }
          else {
            const trimmed = node[2].trim();
            if (trimmed.length === 0) {
              if (sErr.length > 0) sErr += ';'
              sErr += ` node[2] is empty or whitespace-only string`;
              ++iErr
              sLnk = ' null';
            }
            else {
              if (node[2] !== trimmed) {
                if (sErr.length > 0) sErr += ';'
                sErr += ` node[2] text ${JSON.stringify(node[2])} has leading/trailing whitespace`;
                ++iErr
              }

              if (kind === 'hash') {
                if (!/^#.+/.test(trimmed)) {
                  if (sErr.length > 0) sErr += ';'
                  sErr += ` node[2] text ${JSON.stringify(node[2])} must start with '#' and contain a target after it`;
                  ++iErr
                }
              }
              else {
                if (!/\.html$/i.test(trimmed)) {
                  if (sErr.length > 0) sErr += ';'
                  sErr += ` node[2] text ${JSON.stringify(node[2])} does not end with a '.html' (case-insensitive)`;
                  ++iErr
                }
              }

              sLnk = ` ${trimmed}`;
            }
          }
        }
        else if (Array.isArray(node[2])) {
          if (node[2].length === 0) {
            if (isDefault) {
              if (sWrn.length > 0) sWrn += ';'
              sWrn += ` node[2] is an empty array`;
              ++iWrn
            }
            else {
              if (sErr.length > 0) sErr += ';'
              sErr += ` node[2] is an empty array`;
              ++iErr
            }
          }
          else {
            processKids = true;
            sKid = ` ${node[2].length} Children`;
          }
        }
        else {
          if (isDefault) {
            if (node[2] != null) {
              if (sErr.length > 0) sErr += ';'
              sErr += ` node[2] has invalid type '${typStr(node[2])}'`;
              ++iErr
            }
          }
          else {
            if (sErr.length > 0) sErr += ';'
            sErr += ` node[2] has invalid type '${typStr(node[2])}'`;
            ++iErr
          }
        }

        if (sErr.length > 0 && sWrn.length > 0) sErr += ';';

        const childSpaces = spaces + String(lineNo).length + 2;
        if (processKids && parent.length > 0) {
          sGap = ' '.repeat(childSpaces);
          sPar = `${parent}:\n`;
        }

        console.log('%s%c%s%c%s%c%s%c%s%c%s%c%s', sGap, cPar, sPar, cTxt, sTxt, cLnk, sLnk, cKid, sKid, cErr, sErr, cWrn, sWrn);

        if (processKids) {
          const childParents = parent ? `${parent} > ${name}` : name;
          dispBranch(node[2], childSpaces, childParents);
        }
      }
    }
    dispBranch(data);

    if (idx === 0) {
      console.log('--- EMPTY ---');
    }
    else {
      if (iErr > 0 && iWrn > 0) {
        console.log('%s%c%s%c%s%c%s', 'There are ', cErr, `${iErr} Errors`, '', ' and ', cWrn, `${iWrn} Warnings.`)
      }
      else if (iErr > 0) {
        console.log('%s%c%s', 'There are ', cErr, `${iErr} Errors.`);
      }
      else if (iWrn > 0) {
        console.log('%s%c%s', 'There are ', cWrn, `${iWrn} Warnings.`);
      }
    }
  }

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // #endregion 🟥 HELPERS

  // #region 🟩 CONSTANTS
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  // Central container for shared fixed values, grouped constants, and computed top-level identifiers.
  const CONSTS = (() => {

    // DOC_ROOT (Root of the project):
    // - Determine the base path of the currently executing script.
    // - Works for both local file paths and online hosting.
    // - If this does not throw, DOC_ROOT is the directory URL as a string.
    // - Examples:
    //   - https://test.com/docs/doxy-plus.js -> https://test.com/docs/
    //   - file:///F:/Doxy/Dummy/doxy-plus.js -> file:///F:/Doxy/Dummy/
    // - CAUTION:
    //   - Make sure this file (doxy-plus.js) is added via HTML_EXTRA_FILES in Doxyfile.
    //   - Capture this only at top level during script execution, not later inside
    //   callbacks, async code, or delayed code.
    //   - On failure, replaces the page content with a fatal error message and throws.

    // Get the path of the currently executing script.
    const src = document.currentScript && document.currentScript.src;
    if (!src) dispFatalError('Unable to determine DOC_ROOT.');

    // Find the last '/' in path string.
    const pos = src.lastIndexOf('/');
    if (pos < 0) dispFatalError('Invalid script URL for DOC_ROOT.');

    // Get the base path, including the trailing '/' but strip the filename.
    const DOC_ROOT = src.slice(0, pos + 1);

    // Reject '/' because it does not represent a usable path.
    if (DOC_ROOT === '/') dispFatalError('Invalid script URL for DOC_ROOT.');

    // HTML_NAME: The HTML base name of the current page without extension or hash.
    // Example: bar.com/proj/class_foo.html#abc -> class_foo.
    // The 'window.location.pathname' does not include hash part.
    // If 'window.location.pathname.split('/').pop().replace(/\.[^.]*$/, '')' is empty, treat
    // it the same as being on the index.html page. This can happen when DOC_ROOT itself is
    // opened while hosted online, so replacing the empty value with 'index' is correct
    // because the index.html page is shown in that case.
    const HTML_NAME = window.location.pathname.split('/').pop().replace(/\.[^.]*$/, '') || 'index';

    // Class list for presence/absence flags
    const CLS = Object.freeze({
      DEV_HUB_AVAILABLE: 'dp-dev-hub-available',
      HASH_DATA_AVAILABLE: 'dp-hash-data-available',
      HASH_DATA_IS_CODE: 'dp-hash-data-is-code',
      DOC_HEADER_AVAILABLE: 'dp-doc-header-available',
      EMPTY_PAGE_NAV: 'dp-empty-page-nav',
      MAIN_NAV_BELOW_TITLE: 'dp-main-nav-below-title',
      DUAL_NAV_ACTIVE: 'dp-dual-nav-active',
      SHOW_LEAF_PANE: 'dp-show-leaf-pane',
      SHOW_PAGE_NAV: 'dp-show-page-nav',
      AUTO_HIDE_LISTS: 'dp-auto-hide-lists',
      DOXSECTION_CHILDREN: 'dp-doxsection-children',
      MARKDOWN_PAGE_NAV_HEADER_ITEM: 'dp-markdown-page-nav-header-item'
    });

    // Properties for CSS
    const PROP = Object.freeze({
      ROOT_PANE_WIDTH: '--dp-root-pane-width',
      LEAF_PANE_WIDTH: '--dp-leaf-pane-width',
      HEADER_HEIGHT: '--dp-header-height',
      SIDE_NAV_WIDTH: '--dp-side-nav-width',
      FOOTER_HEIGHT: '--dp-footer-height',
      DOC_HEADER_HEIGHT: '--dp-doc-header-height',
      SEARCH_FIELD: '--dp-search-field'
    });

    // Values
    const VAL = Object.freeze({
      MIN_W: 100,                          // Minimum width (px) of panes
      GUTTER_W: 250,                       // Gutter width (px) on right side in dual-nav
      DESKTOP_VIEW_MIN_WIDTH: 768,         // Max width (px) of window for Desktop view
      LEAF_PANE_AUTO_HIDE_THRESHOLD: 1500, // Threshold (px) width for leaf pane auto hide
      PANE_WIDTH_SAVE_DEBOUNCE_MS: 500,    // Timeout (ms) to save pane widths when dragging/resizing
      TIMEOUT_MS: 2000,                    // Timeout (ms) for waiting (not used in waitFor() helper)
      BUCKET_THRESHOLD: 20                 // Max count of flat list after which Alphabetic groups are formed
    });

    // Common DOM selectors used throughout the script.
    const SEL = Object.freeze({
      TOP: '#top',
      TITLE_AREA: '#titlearea',
      SIDE_NAV: '#side-nav',
      NAV_PATH: '#nav-path',
      PAGE_NAV: '#page-nav',
      CONTAINER: '#container',
      DOC_CONTENT: '#doc-content',
      DP_ROOT_PANE: '#dp-root-pane',
      DP_LEAF_SHOW: '#dp-leaf-show',
      DP_ROOT_RESIZER: '#dp-root-pane-resizer',
      DP_LEAF_RESIZER: '#dp-leaf-pane-resizer',
      SEARCH_RESULTS_WIN: '#MSearchResultsWindow',
      PAGE_CONTENT: '#doc-content > div.contents, #doc-content > div > div.contents > div.textblock'
    });

    // Expose the frozen constant groups and computed identifiers.
    return Object.freeze({
      CLS,
      PROP,
      VAL,
      SEL,
      DOC_ROOT,
      HTML_NAME
    });
  })();

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // #endregion 🟥 CONSTANTS

  // #region 🟩 STORAGE
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  // Namespace and expiry based storage (both using store.js) for the project
  const storage = (() => {

    const STORE_JS_EXPIRY_PREFIX = '__storejs___storejs_'; // Prefix for expiry key name in local storage
    const STORE_JS_EXPIRY_SEPARATOR = '_expire_mixin_'; // Separator for expiry key name in local storage
    const STORE_JS_NORMAL_PREFIX = '__storejs_'; // Prefix for value keyname in local storage
    const TIME_TO_LIVE = 30 * 24 * 60 * 60 * 1000; // 30 days in ms, time until stored data is considered stale

    // NOTE: No underscore in key name because store.js prefix and separator uses underscores and
    // we would like to not mingle our underscores to have easier separation in key names.
    const KEY__HTML_DATA = 'html-data'; // Key by which HTML data is stored
    const KEY__HTML_IGNORED = 'html-ignored'; // Array of HTML files ignored in HTML data
    const KEY__HTML_NOT_FOUND = 'html-not-found'; // Array of HTML pages not found in Doxygen NAVTREE
    const KEY__EXPIRED_DATA_LAST_PURGE_DATE = 'expired-data-last-purge-date'; // Last purge date storage key
    const KEY__GENERATION_TIMESTAMP = 'generation-timestamp'; // Generation timestamp as provided by doxygen.css
    const PREFIX__HASH_DATA = 'hash-data--'; // Prefix for hash data of the current HTML_NAME
    const PREFIX__HASH_DATA_IS_CODE = 'hash-data-is-code--'; // Prefix to know if hash data for for code
    const PREFIX__PAGE_SCROLL_VPOS = 'page-scroll-vpos--'; // Previously held scroll vertical position
    const PREFIX__PAGE_HAS_DOXSECTIONS = 'page-has-doxsections--';

    // This is the namespace for this project. All keys are stored in this namespace.
    // NOTE: store.js namespaces can only contain letters, digits, underscores, and dashes.
    // NOTE: We intentionally avoid underscores because store.js uses underscores in its
    //       prefix and separator format. Avoiding them makes key names easier to read
    //       and easier to split when retrieving namespace-key pairs from storage.
    const PROJ_NAMESPACE = (() => {

      // Create a stable hash from the original CONSTS.DOC_ROOT.
      // - The same CONSTS.DOC_ROOT always produces the same hash.
      // - The hash is always 8 hexadecimal characters.
      let hashSeed = 2166136261;
      for (let ii = 0; ii < CONSTS.DOC_ROOT.length; ++ii) {
        hashSeed ^= CONSTS.DOC_ROOT.charCodeAt(ii);
        hashSeed = Math.imul(hashSeed, 16777619);
      }
      const hash = (hashSeed >>> 0).toString(16).padStart(8, '0');

      // Sanitize CONSTS.DOC_ROOT to create a human-readable base string.
      const base = CONSTS.DOC_ROOT
        .replace(/[^A-Za-z0-9-]+/g, '-')
        .replace(/-+/g, '-')
        .replace(/^-+|-+$/g, '');

      // Combine both parts to create the project namespace.
      // Fall back to "proj" if sanitization removes the entire base.
      return `${base || 'proj'}--${hash}`;
    })();

    // The store.js namespaced variable for direct use
    const PROJ_STORAGE = store.namespace(PROJ_NAMESPACE);

    // Returns key list divided by namespace on this origin (only keys with expiry are included)
    function getAllNamespaceKeyPairs() {
      const map = new Map();

      for (const fullKey of Object.keys(localStorage)) {
        if (!fullKey.startsWith(STORE_JS_EXPIRY_PREFIX)) continue;

        const sepPos = fullKey.indexOf(STORE_JS_EXPIRY_SEPARATOR, STORE_JS_EXPIRY_PREFIX.length);
        if (sepPos === -1) continue;

        const namespace = fullKey.slice(STORE_JS_EXPIRY_PREFIX.length, sepPos);
        const actualKey = fullKey.slice(sepPos + STORE_JS_EXPIRY_SEPARATOR.length);
        if (!namespace || !actualKey) continue;

        if (!map.has(namespace)) map.set(namespace, []);
        map.get(namespace).push(actualKey);
      }

      const list = [];
      for (const [namespace, keyList] of map) {
        list.push({ proj: namespace, keys: keyList });
      }

      return list;
    }

    // Displays keys stored in origin storage using store.js
    function dispOriginStorage() {
      const list = getAllNamespaceKeyPairs();
      list.sort((a, b) => a.proj.localeCompare(b.proj));
      for (const node of list) {
        node.keys.sort((a, b) => a.localeCompare(b));
      }

      let total = 0;
      console.group('Origin Storage');
      console.log(`- The key-value pairs stored (using store.js) on this origin`,
        `\n- Examples of the same origin:`,
        `\n  - some.com/proj_a and some.com/proj_b share the same origin: some.com`,
        `\n  - D:/file/proj_a and E:/other/proj_b may share the same local origin, depending on the browser`,
        `\n- Data is stored using store.js features:`,
        `\n  - Namespace: Different projects on the same origin can store different values for the same keys`,
        `\n    (Namespace is obtained using project url)`,
        `\n  - Expiry: Each key has its own expiry and is removed when accessed after it has expired`,
        `\n- Display format: '[Expiry] #. Key: Value'`);
      for (const node of list) {
        let idx = 0;
        const grpName = node.proj === PROJ_NAMESPACE ? `Namespace: ${node.proj} (This Project)` : `Namespace: ${node.proj}`;
        console.log('%c' + grpName + ':', 'font-weight: bold');
        const projStore = store.namespace(node.proj);
        for (const name of node.keys) {
          const expiresAt = projStore.getExpiration(name);
          const expiry = expiresAt != null ? formatDateTime(expiresAt) : null;
          const value = projStore.get(name);
          const val = Array.isArray(value) ? `Array(${value.length})` : value;
          console.log('%c%s%c%s%c%s', 'color: GrayText;', `[${expiry}]`, '', ` ${++idx}. ${name}: `, 'color: LinkText;', val);
        }
        total += idx;
      }
      console.groupEnd();
      if (total === 0) return 'Origin Storage: Empty';
      else return `Origin Storage: ${total} keys`;
    }

    // Displays keys stored for this project using store.js
    function dispProjectStorage() {
      const list = getAllNamespaceKeyPairs();
      let idx = 0;
      console.group('Project Storage');
      console.log(`- The key-value pairs stored (using store.js) for this project on this origin`,
        `\n- Examples of the same origin:`,
        `\n  - some.com/proj_a and some.com/proj_b share the same origin: some.com`,
        `\n  - D:/file/proj_a and E:/other/proj_b may share the same local origin, depending on the browser`,
        `\n- Data is stored using store.js features:`,
        `\n  - Namespace: Different projects on the same origin can store different values for the same keys`,
        `\n    (Namespace is obtained using project url)`,
        `\n  - Expiry: Each key has its own expiry and is removed when accessed after it has expired`,
        `\n- Display format: '[Expiry] #. Key: Value'`);
      for (const node of list) {
        if (node.proj === PROJ_NAMESPACE) {
          node.keys.sort((a, b) => a.localeCompare(b));
          console.log('%c%s', 'font-weight: bold;', `Namespace: ${PROJ_NAMESPACE}`);
          for (const name of node.keys) {
            const expiresAt = PROJ_STORAGE.getExpiration(name);
            const expiry = expiresAt != null ? formatDateTime(expiresAt) : null;
            const value = PROJ_STORAGE.get(name);
            const val = Array.isArray(value) ? `Array(${value.length})` : value;
            console.log('%c%s%c%s%c%s', 'color: GrayText;', `[${expiry}]`, '', ` ${++idx}. ${name}: `, 'color: LinkText;', val);
          }
          break;
        }
      }
      console.groupEnd();
      if (idx === 0) return 'Project Storage: Empty';
      else return `Project Storage: ${idx} keys`;
    }

    // Displays local storage (including ones stored through store.js)
    function dispLocalStorage() {
      let idx = 0;
      console.group('Local Storage');
      console.log(`- The current contents of the browser's localStorage`,
        `\n- Includes all keys from all projects on this origin`,
        `\n- Examples of the same origin:`,
        `\n  - some.com/proj_a and some.com/proj_b share the same origin: some.com`,
        `\n  - D:/file/proj_a and E:/other/proj_b may share the same local origin, depending on the browser`,
        `\n- Keys starting with '__storejs_' are key values from store.js keys`,
        `\n  - These keys might appear garbled (mojibake) because of incorrect decoding while reading it`,
        `\n  - These keys should only be read using store.js to get its correct value`,
        `\n    (Use 'Origin Storage' or 'Project Storage' to read these keys)`,
        `\n- Keys starting with '__storejs___storejs_' are expiry timestamp (ms) keys from store.js`,
        `\n- There is no expiry feature for localStorage itself`,
        `\n- Display format: '#. key: value'`);
      for (const fullKey of Object.keys(localStorage).sort((a, b) => a.localeCompare(b))) {
        const value = localStorage.getItem(fullKey);
        let val = Array.isArray(value) ? `Array(${value.length})` : value;
        if (typeof val === 'string' && /[^\x00-\x7F]/.test(val) && val.length > 32) {
          val = val.slice(0, 29) + '...';
        }
        console.log('%s%c%s', `${++idx}. ${fullKey}: `, 'color: LinkText;', val);
      }
      console.groupEnd();
      if (idx === 0) return 'Local Storage: Empty';
      else return `Local Storage: ${idx} keys`;
    }

    // Displays local storage non-store.js
    function dispLocalStorageNonStoreJS() {
      let idx = 0;
      console.group('Local Storage (excluding store.js keys)');
      console.log(`- The current contents of the browser's localStorage excluding keys stored using store.js`,
        `\n- Includes all keys from all projects on this origin`,
        `\n- Examples of the same origin:`,
        `\n  - some.com/proj_a and some.com/proj_b share the same origin: some.com`,
        `\n  - D:/file/proj_a and E:/other/proj_b may share the same local origin, depending on the browser`,
        `\n- Excludes keys starting with '__storejs_' (since these are store.js keys)`,
        `\n- There is no expiry feature for localStorage itself`,
        `\n- Display format: '#. key: value'`);
      for (const fullKey of Object.keys(localStorage).sort((a, b) => a.localeCompare(b))) {
        if (!fullKey.startsWith(STORE_JS_NORMAL_PREFIX)) {
          const value = localStorage.getItem(fullKey);
          let val = Array.isArray(value) ? `Array(${value.length})` : value;
          if (typeof val === 'string' && /[^\x00-\x7F]/.test(val) && val.length > 32) {
            val = val.slice(0, 29) + '...';
          }
          console.log('%s%c%s', `${++idx}. ${fullKey}: `, 'color: LinkText;', val);
        }
      }
      console.groupEnd();
      if (idx === 0) return 'Local Storage (excluding store.js keys): Empty';
      else return `Local Storage (excluding store.js keys): ${idx} keys`;
    }

    // Displays session storage (including ones stored through store.js)
    function dispSessionStorage() {
      let idx = 0;
      console.group('Session Storage');
      console.log(`- The current contents of the browser's sessionStorage`,
        `\n- sessionStorage is tab based:`,
        `\n  - Each tab has its own sessionStorage`,
        `\n  - sessionStorage is destroyed on tab close or browser restart`,
        `\n- Keys starting with '__storejs_' are key values from store.js keys`,
        `\n  - These keys might appear garbled (mojibake) because of incorrect decoding while reading it`,
        `\n  - These keys should only be read using store.js to get its correct value`,
        `\n    (Use 'Origin Storage' or 'Project Storage' to read these keys)`,
        `\n- Keys starting with '__storejs___storejs_' are expiry timestamp (ms) keys from store.js`,
        `\n- There is no expiry feature for sessionStorage itself`,
        `\n- Display format: '#. key: value'`);
      for (const fullKey of Object.keys(sessionStorage).sort((a, b) => a.localeCompare(b))) {
        const value = sessionStorage.getItem(fullKey);
        let val = Array.isArray(value) ? `Array(${value.length})` : value;
        if (typeof val === 'string' && /[^\x00-\x7F]/.test(val) && val.length > 32) {
          val = val.slice(0, 29) + '...';
        }
        console.log('%s%c%s', `${++idx}. ${fullKey}: `, 'color: LinkText;', val);
      }
      console.groupEnd();
      if (idx === 0) return 'Session Storage: Empty';
      else return `Session Storage: ${idx} keys`;
    }

    // Displays session storage non-store.js
    function dispSessionStorageNonStoreJS() {
      let idx = 0;
      console.group('Session Storage (excluding store.js keys)');
      console.log(`- The current contents of the browser's sessionStorage excluding keys stored using store.js`,
        `\n- sessionStorage is tab based:`,
        `\n  - Each tab has its own sessionStorage`,
        `\n  - sessionStorage is destroyed on tab close or browser restart`,
        `\n- Excludes keys starting with '__storejs_' (since these are store.js keys)`,
        `\n- There is no expiry feature for sessionStorage itself`,
        `\n- Display format: '#. key: value'`);
      for (const fullKey of Object.keys(sessionStorage).sort((a, b) => a.localeCompare(b))) {
        if (!fullKey.startsWith(STORE_JS_NORMAL_PREFIX)) {
          const value = sessionStorage.getItem(fullKey);
          let val = Array.isArray(value) ? `Array(${value.length})` : value;
          if (typeof val === 'string' && /[^\x00-\x7F]/.test(val) && val.length > 32) {
            val = val.slice(0, 29) + '...';
          }
          console.log('%s%c%s', `${++idx}. ${fullKey}: `, 'color: LinkText;', val);
        }
      }
      console.groupEnd();
      if (idx === 0) return 'Session Storage (excluding store.js keys): Empty';
      else return `Session Storage (excluding store.js keys): ${idx} keys`;
    }

    // Removes all keys on this origin
    function clearAllKeys() {
      console.log('Check browser page to confirm...');
      const confirmed = window.confirm('Clear all Keys?\nThis will remove all localStorage keys stored on this origin and all sessionStorage keys for this tab.');
      if (confirmed) {
        localStorage.clear();
        sessionStorage.clear();
        return 'All localStorage keys stored on this origin and all sessionStorage keys for this tab has been cleared';
      }
      else {
        return 'Clearing all keys cancelled';
      }
    }

    // Remove all localStoarge keys on this origin
    function clearLocalStorage() {
      console.log('Check browser page to confirm...');
      const confirmed = window.confirm('Clear localStorage keys?\nThis will remove all localStorage keys stored on this origin.');
      if (confirmed) {
        localStorage.clear();
        return 'All localStorage keys stored on this origin has been cleared';
      }
      else {
        return 'Clearing localStorage keys cancelled';
      }
    }

    // Remove all sessionStorage keys on this origin
    function clearSessionStorage() {
      console.log('Check browser page to confirm...');
      const confirmed = window.confirm('Clear sessionStorage keys?\nThis will remove all sessionStorage keys for this tab.');
      if (confirmed) {
        sessionStorage.clear();
        return 'All sessionStorage keys for this tab has been cleared';
      }
      else {
        return 'Clearing sessionStorage keys cancelled';
      }
    }

    // Removes all keys stored using store.js on this origin
    function clearOriginStorage() {
      console.log('Check browser page to confirm...');
      const confirmed = window.confirm('Clear origin storage keys?\nThis will remove all keys stored on this origin using store.js.');
      if (confirmed) {
        let idx = 0;
        const list = getAllNamespaceKeyPairs();
        for (const node of list) {
          const projStore = store.namespace(node.proj);
          for (const name of node.keys) {
            ++idx;
            projStore.remove(name);
          }
        }
        return `Cleared ${idx} keys stored using store.js on this origin`;
      }
      else {
        return 'Clearing origin storage keys cancelled';
      }
    }

    // Removes all keys stored using store.js for this project
    function clearProjectStorage() {
      console.log('Check browser page to confirm...');
      const confirmed = window.confirm('Clear project storage keys?\nThis will remove all store.js keys stored for this project on this origin.');
      if (confirmed) {
        let idx = 0;
        const list = getAllNamespaceKeyPairs();
        for (const node of list) {
          if (node.proj === PROJ_NAMESPACE) {
            for (const name of node.keys) {
              ++idx;
              PROJ_STORAGE.remove(name);
            }
            break;
          }
        }
        return `Cleared ${idx} keys stored for this project using store.js on this origin`;
      }
      else {
        return 'Clearing project storage keys cancelled';
      }
    }

    // Purge any key + data which is stored on this origin and has been expired
    function purgeExpired() {
      // Use the UTC calendar date in YYYY-MM-DD form so the once-per-day
      // guard is compact and timezone-stable.
      const today = new Date().toISOString().slice(0, 10);

      // Skip the purge if it already completed today.
      const lastPurged = localStorage.getItem(KEY__EXPIRED_DATA_LAST_PURGE_DATE);
      if (lastPurged === today) return;

      // Collect store.js namespace/key pairs for entries that use the
      // expire mixin encoding in localStorage.
      const list = getAllNamespaceKeyPairs();

      // Remove expired data
      const now = Date.now();
      for (const node of list) {
        const nsTemp = store.namespace(node.proj);
        for (const keyName of node.keys) {
          const exp = nsTemp.getExpiration(keyName);
          if (typeof exp === 'number' && exp <= now) {
            nsTemp.remove(keyName);
          }
        }
      }

      // Record that today's purge completed.
      localStorage.setItem(KEY__EXPIRED_DATA_LAST_PURGE_DATE, today);
    }

    // Run purgeExpired() when the browser becomes idle when supported
    if ('requestIdleCallback' in window) {
      requestIdleCallback(purgeExpired, { timeout: 5000 });
    } else {
      setTimeout(purgeExpired, 5000); // Use delayed fallback if requestIdleCallback is not available
    }

    // If the Doxygen generation date-time does not match the stored value then 
    // delete all HTML, ignored HTML and hash data and save the new date-time.
    // Previously, the date-time was obtained using window.DOXY_PLUS_DATE_TIME
    // and since this is generated for each html page separately, there was difference
    // between the values obtained using window.DOXY_PLUS_DATE_TIME between two
    // html pages. A better solution is to use the css variable '--timestamp'. This
    // is defined at the very end of doxygen.css file and contains the generation
    // timestamp and since all different html pages share this doxygen.css file
    // all of it gets the same timestamp.
    // --- For details see the DATE-TIME ISSUE section at the top of this page ---
    let dateTime = '';
    (() => {
      // Since the --timestamp is like 'Thu Apr 16 2026 20:32:59' in the doxygen.css
      // i.e. a string variable, the curTimeStamp becomes double quoted string like 
      // "'Thu Apr 16 2026 20:32:59'" here. When stored it looks the same that is
      // starting with "' and ending with '"". So we use .replace(/^['"]|['"]$/g, '') to
      // remove only one ' or " character and only from both beginning and end. This way
      // curTimeStamp remains simple string and is stored that way.
      const curTimeStamp = getComputedStyle(document.documentElement)
        .getPropertyValue('--timestamp') // get --timestamp from doxygen.css, it always returns a string
        .trim() // remove leading and trailing spaces
        .replace(/^['"]|['"]$/g, ''); // Remove one leading and one trailing quote, if present.

      if (curTimeStamp.length === 0) {
        // This throws and shows the error, so nothing more is needed after dispFatalError call.
        dispFatalError('Storage: Current timestamp from CSS variable "--timestamp" is invalid.');
      }

      const storedTimeStamp = PROJ_STORAGE.get(KEY__GENERATION_TIMESTAMP);
      if (storedTimeStamp !== curTimeStamp) {
        PROJ_STORAGE.remove(KEY__HTML_DATA);
        PROJ_STORAGE.remove(KEY__HTML_IGNORED);
        PROJ_STORAGE.remove(KEY__HTML_NOT_FOUND);
        const list = getAllNamespaceKeyPairs();
        for (const node of list) {
          if (node.proj === PROJ_NAMESPACE) {
            for (const keyName of node.keys) {
              if (keyName.startsWith(PREFIX__HASH_DATA)
                || keyName.startsWith(PREFIX__HASH_DATA_IS_CODE)
                || keyName.startsWith(PREFIX__PAGE_SCROLL_VPOS)
                || keyName.startsWith(PREFIX__PAGE_HAS_DOXSECTIONS)) {
                PROJ_STORAGE.remove(keyName);
              }
            }
            break;
          }
        }
      }
      PROJ_STORAGE.set(KEY__GENERATION_TIMESTAMP, curTimeStamp, Date.now() + TIME_TO_LIVE);
      dateTime = curTimeStamp;
    })();

    // Extend the expiry time for a key
    function extendExpiryPrivate(name) {
      const exp = PROJ_STORAGE.getExpiration(name);
      if (typeof exp === 'number') {
        const fullKey = `${STORE_JS_EXPIRY_PREFIX}${PROJ_NAMESPACE}${STORE_JS_EXPIRY_SEPARATOR}${name}`;
        localStorage.setItem(fullKey, String(Date.now() + TIME_TO_LIVE));
      }
    }

    // Construct a key with a given name
    function makeKey(name) {
      return Object.freeze({

        // name of the key
        name,

        // Load the data from storage, it automatically identifies normal data types 
        // (e.g. array and numbers) and converts to it.
        load(defVal = null) {
          const loaded = PROJ_STORAGE.get(name);
          if (loaded === undefined) return defVal;
          extendExpiryPrivate(name);
          return loaded;
        },

        // Save the value and extend the expiry
        save(val) {
          PROJ_STORAGE.set(name, val, Date.now() + TIME_TO_LIVE);
        },

        // Remove key + data from storage
        remove() {
          PROJ_STORAGE.remove(name);
        },

        // Explicit function to just extend the expiry
        extendExpiry() { extendExpiryPrivate(name); }
      });
    }

    // Internals of storage
    const internals = Object.freeze({
      timeToLiveMs: TIME_TO_LIVE,
      namespace: PROJ_NAMESPACE,
      dateTime,
      dispOriginStorage,
      dispProjectStorage,
      dispLocalStorage,
      dispLocalStorageNonStoreJS,
      dispSessionStorage,
      dispSessionStorageNonStoreJS,
      clearAllKeys,
      clearLocalStorage,
      clearSessionStorage,
      clearOriginStorage,
      clearProjectStorage
    });

    // Expose internals and keys (add new keys here)
    return Object.freeze({
      internals,
      htmlData: makeKey(KEY__HTML_DATA),
      htmlIgnored: makeKey(KEY__HTML_IGNORED),
      htmlNotFound: makeKey(KEY__HTML_NOT_FOUND),
      htmlPrevName: makeKey('html-prev-name'),
      hashData: makeKey(PREFIX__HASH_DATA + CONSTS.HTML_NAME),
      hashDataIsCode: makeKey(PREFIX__HASH_DATA_IS_CODE + CONSTS.HTML_NAME),
      pageScrollVPos: makeKey(PREFIX__PAGE_SCROLL_VPOS + CONSTS.HTML_NAME),
      pageHasDoxsections: makeKey(PREFIX__PAGE_HAS_DOXSECTIONS + CONSTS.HTML_NAME),
      dualNav: makeKey('dual-nav'),
      showPageNav: makeKey('show-page-nav'),
      showLeafPane: makeKey('show-leaf-pane'),
      autoHideLists: makeKey('auto-hide-lists'),
      rootPaneWidth: makeKey('root-pane-width'),
      leafPaneWidth: makeKey('leaf-pane-width')
    });

  })();

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // #endregion 🟥 STORAGE

  // #region 🟩 VARIABLES
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  // Mutable runtime state used across the script.
  const vars = {

    // Cached DOM elements looked up from CONSTS.SEL...
    els: {},

    // User/UI state loaded once from storage during startup.
    dualNav: storage.dualNav.load(true),
    showPageNav: storage.showPageNav.load(true),
    showLeafPane: storage.showLeafPane.load(true),
    autoHideLists: storage.autoHideLists.load(true),

    // Marks current page as a Markdown page with doxsections
    pageHasDoxsections: false,

    // Persisted pane widths.
    rootPaneWidth: storage.rootPaneWidth.load(200),
    leafPaneWidth: storage.leafPaneWidth.load(400),

    // Shared data arrays; keep references stable and mutate contents when needed.
    htmlData: [],
    hashData: [],

    // Name used when exposing the console helper object on window.
    consoleObjectName: '',

    // Cache of script-load promises keyed by absolute URL.
    scriptLoadMap: new Map()
  };

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // #endregion 🟥 VARIABLES

  // #region 🟩 DOXYGEN NAVTREE
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  // Returns a fully loaded clone of Doxygen NAVTREE, or [] on timeout.
  async function buildDoxygenNavtree(prune = true) {
    // Steps:
    // 1. Wait until window.NAVTREE exists and window.NAVTREE[0][2] is a 
    //    non-empty array (timeout after CONSTS.VAL.TIMEOUT_MS).
    // 2. Recursively clone the tree structure into a new array.
    // 3. Dynamically load any external JS files for deferred child nodes.
    // 4. Recursively attach those loaded children.
    // 5. If 'prune' is true Prune hash entries from the cloned NAVTREE, e.g. remove links 
    //    like "file.html#anchor".
    // 6. Return the cloned navtree.

    // Wait for NAVTREE
    const start = performance.now();
    while (!Array.isArray(window.NAVTREE) || !Array.isArray(window.NAVTREE?.[0]?.[2]) || window.NAVTREE[0][2].length === 0) {
      const elapsed = performance.now() - start;
      if (elapsed > CONSTS.VAL.TIMEOUT_MS) {
        dispFatalError(`buildDoxygenNavtree(): Timed out waiting for a non-empty "window.NAVTREE[0][2]" after ${Math.round(elapsed)} ms`);
        return [];
      }
      //console.log(`buildDoxygenNavtree(): Waiting for non-empty "window.NAVTREE[0][2]" after ${Math.round(elapsed)} ms...`);
      await new Promise((resolve) => { setTimeout(resolve, 16); });
    }

    // Load a script only once and share the same promise across callers.
    function loadScript(relUrl) {
      // - Build one absolute URL so different relative forms map to one key.
      // - Return the existing promise when the same script was already requested.
      // - Remove the failed entry from the cache so a later retry is possible.

      // Build the absolute URL.
      const fullUrl = new URL(relUrl, CONSTS.DOC_ROOT).href;

      // Reuse the existing promise when this absolute URL was already requested.
      const existingPromise = vars.scriptLoadMap.get(fullUrl);
      if (existingPromise) return existingPromise;

      const promise = new Promise((resolve, reject) => {
        // Create and configure a script element.
        const s = document.createElement('script');
        s.src = fullUrl;
        s.async = true;

        // when the script finishes loading, call resolve()
        s.onload = () => { resolve(); };

        // if the script fails to load, delete from map, remove it and call reject(err)
        s.onerror = (err) => {
          vars.scriptLoadMap.delete(fullUrl);
          s.remove();
          reject(err);
        };

        // Insert the script element into the page and start the request.
        document.head.appendChild(s);
      });

      // Add script to the map and return the promise
      vars.scriptLoadMap.set(fullUrl, promise);
      return promise;
    }

    // Walks the cloned tree, loads deferred children, and replaces string refs
    function loadChildren(branch) {
      if (!Array.isArray(branch) || branch.length === 0) return Promise.resolve();
      const promises = [];
      for (const node of branch) {
        if (!Array.isArray(node)) continue;
        const h = node[1]; // Get the href
        const c = node[2]; // Child reference or child array.
        if (typeof c === 'string' && c.length > 0) {

          // c is a script base name: load it then replace node[2] with the loaded array
          // Check if the h is an anchor link (contains '#').
          // Inlined elements link to anchors, not new pages.
          const isAnchorLink = h && h.includes('#');

          // WARNING: Do not remove this check for anchor link. For inlined element
          // (e.g., INLINE_SIMPLE_STRUCTS = YES) there is still a script in node[2]
          // but loading that script leads to errors because the file it points to
          // does not exist.
          if (isAnchorLink) {
            // This is an inlined element (e.g., INLINE_SIMPLE_STRUCTS = YES).
            // It won't have a separate .js file for children.
            // Set children to an empty array and skip loading.
            node[2] = [];
          } else {
            // This is a regular page, load the script as before.
            // c is a script base name: load it then replace node[2] with the cloned loaded array
            promises.push(
              loadScript(c + '.js')
                .then(() => {
                  let arr = window[c];
                  if (!Array.isArray(arr)) arr = window[c.split('/').pop()];

                  if (!Array.isArray(arr)) {
                    console.warn(`buildDoxygenNavtree() - loadChildren(): "${c}.js" loaded, but no valid array global was found for "${c}"`);
                    node[2] = [];
                    return;
                  }

                  node[2] = deepCloneArray(arr);
                  return loadChildren(node[2]);
                })
                .catch((err) => {
                  console.warn(`buildDoxygenNavtree() - loadChildren(): Failed to resolve children for "${c}.js"`, err);
                  node[2] = [];
                })
            );
          }
        }
        else if (Array.isArray(c) && c.length > 0) {
          // Already an array: recurse
          promises.push(loadChildren(c));
        }
      }
      return Promise.all(promises);
    }

    // Clone the default Doxygen NAVTREE
    const clonedTree = deepCloneArray(window.NAVTREE);

    // Load all deferred children; not all NAVTREE data is present up front.
    await loadChildren(clonedTree);

    // Doxygen changes certain characters like '&' to '&amp;', '<' to '&lt;', etc.
    // To change it back we use assign the text to a 'textarea' and replace the
    // text with its value. This automatically changes the text back to original.
    const decodeText = document.createElement('textarea');
    function decodeName(text) {
      if (typeof text !== 'string') return text;
      decodeText.innerHTML = text;
      return decodeText.value;
    }

    if (prune) {
      // Prune cloned tree in place.
      function pruneClonedTree(branch) {
        if (!Array.isArray(branch) || branch.length === 0) return;

        for (let ii = branch.length - 1; ii >= 0; --ii) {

          // Remove nodes that are not arrays
          if (!Array.isArray(branch[ii])) {
            branch.splice(ii, 1);
            continue;
          }

          // Normalize length to 3 entries only
          branch[ii].length = 3;

          // Remove nodes whos first entry is not string
          if (typeof branch[ii][0] !== 'string') {
            branch.splice(ii, 1);
            continue;
          }

          // Set the name back to original
          branch[ii][0] = decodeName(branch[ii][0]);

          // Remove nodes with empty names
          if (branch[ii][0].trim().length === 0) {
            branch.splice(ii, 1);
            continue;
          }

          // Prune child nodes if present.
          pruneClonedTree(branch[ii][2]);
          if (!Array.isArray(branch[ii][2]) || branch[ii][2].length === 0) branch[ii][2] = null;

          // Validate href: keep only valid html links, otherwise set to null
          if (typeof branch[ii][1] !== 'string' || branch[ii][1].includes('#') || !/\.html$/i.test(branch[ii][1])) branch[ii][1] = null;

          // Remove nodes that have no href and no kids
          if (branch[ii][1] === null && branch[ii][2] === null) branch.splice(ii, 1);
        }
      }
      pruneClonedTree(clonedTree);
    }
    else {
      // Only decode names back to its original text
      function decodeAllNames(branch) {
        if (!Array.isArray(branch) || branch.length === 0) return;
        for (const node of branch) {
          if (!Array.isArray(node)) continue;
          if (typeof node[0] === 'string') node[0] = decodeName(node[0]);
          if (Array.isArray(node[2]) && node[2].length > 0) decodeAllNames(node[2]);
        }
      }
      decodeAllNames(clonedTree);
    }

    //console.log('buildDoxygenNavtree(): SUCCESS');
    return clonedTree;
  }

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // #endregion 🟥 DOXYGEN NAVTREE

  // #region 🟩 INIT HTML DATA
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  // Initialize vars.htmlData array
  async function initHtmlData() {

    // Reset the HTML data before rebuilding it, while maintaining its reference
    vars.htmlData.length = 0;

    // Check whether the page should be redirected.
    function checkRedirect(tree) {
      // tree: Array with nodes in the shape [label, scope, hrefOrKids].
      // The href stored here is the default NAVTREE href generated by Doxygen,
      // for example: d2/d0d/classnpi_1_1_ref_ptr.html.
      // We match only the base name, for example: classnpi_1_1_ref_ptr.

      // Gate 1: Only proceed on a truly fresh tab (not after reload/back/forward)
      const isStale = (sessionStorage.getItem('is_stale') === 'true');
      sessionStorage.setItem('is_stale', 'true');
      if (isStale) return false; // Not a fresh load: no redirect

      // Gate 2: Only on standard navigation (ignore reloads and history traversals)
      const navType = performance.getEntriesByType('navigation')[0]?.type ?? 'navigate';
      if (navType !== 'navigate') return false; // Reload or back/forward: no redirect

      // Gate 3: Only redirect on landing page (root or 'index.html')
      const pName = window.location.pathname;
      if (!pName.endsWith('/') && !pName.endsWith('/index.html')) return false; // Not on landing page: no redirect

      // Gate 4: Stored HTML must exist, be non-empty, and not be the current HTML name
      const htmlPrevName = storage.htmlPrevName.load();
      if (!htmlPrevName
        || typeof htmlPrevName !== 'string'
        || htmlPrevName.length === 0
        || htmlPrevName === CONSTS.HTML_NAME) return false; // No redirect

      // Find a matching HTML page name.
      function findLastHtmlPage(branch) {
        if (!Array.isArray(branch) || branch.length === 0) return false;
        for (const node of branch) {
          if (typeof node[2] === 'string') {
            const name = node[2].split('/').pop().replace(/\.[^.]*$/, '');
            if (name === htmlPrevName) {
              window.location.replace(CONSTS.DOC_ROOT + node[2]); // Redirect
              return true; // Stop after the first match
            }
          }
          else if (Array.isArray(node[2])) {
            if (findLastHtmlPage(node[2])) return true; // Propagate success upward
          }
        }
        return false;
      }

      // Return whether a matching page was found.
      return findLastHtmlPage(tree);
    }

    // Load previous HTML data if available
    const prevHtmlData = storage.htmlData.load();

    // Check for previous HTML data being a non-empty array
    if (Array.isArray(prevHtmlData) && prevHtmlData.length > 0) {

      // Check if the data obtained from storage is valid
      if (checkTreeData(prevHtmlData, 'html')) {

        // Check for redirection, if true return because the url will be redirected
        if (checkRedirect(prevHtmlData)) return;

        // Add dev hub class in document body to detect it in CSS file
        if (prevHtmlData[prevHtmlData.length - 1][0] === 'Dev Hub') {
          document.body.classList.add(CONSTS.CLS.DEV_HUB_AVAILABLE);
        }

        // Extend expiry timeout of ignored not found HTML arrays
        storage.htmlIgnored.extendExpiry();
        storage.htmlNotFound.extendExpiry();

        // Save current HTML name for future use
        storage.htmlPrevName.save(CONSTS.HTML_NAME);

        // Assign loaded HTML data to vars.htmlData array, freeze it and return
        vars.htmlData.push(...prevHtmlData);
        deepFreeze(vars.htmlData);
        return;
      }
      else {
        storage.htmlData.remove();
        storage.htmlIgnored.remove();
        storage.htmlNotFound.remove();
      }
    }

    // Get the fully built pruned Doxygen NAVTREE
    const prunedNavTree = await buildDoxygenNavtree(true);

    // Check whether prunedNavTree is an array
    if (!Array.isArray(prunedNavTree)) {
      dispFatalError('initHtmlData(): Pruned Doxygen NAVTREE is not an array.');
      return;
    }

    // Check whether prunedNavTree is empty
    if (prunedNavTree.length === 0) {
      dispFatalError('initHtmlData(): Pruned Doxygen NAVTREE is empty.');
      return;
    }

    // For prunedNavTree these are guaranteed:
    // - node[0] is a non-empty string.
    // - node[1] is either null or a non-empty string ending in .html.
    // - node[2] is either null or an array of valid child nodes.
    // - At least one of node[1] and node[2] is non-null.
    // So, we will be reducing the number of redundant checks from here onwards.

    // Map of all nodes that is to be obtained from prunedNavTree
    const pending = new Map([
      ['mainPage', { Link: 'index.html', Path: [] }],
      ['requirements', { Link: 'requirements.html', Path: [] }],
      ['depRequired', { Link: 'dp_dep_required.html', Path: [] }],
      ['dependencies', { Link: 'dp_dep_dir_dependencies.html', Path: [] }],
      ['architecture', { Link: 'dp_arch_dir_architecture.html', Path: [] }],
      ['namespaceList', { Link: 'namespaces.html', Path: ['Namespaces', 'Namespace List'] }],
      ['namespaceMems', { Link: null, Path: ['Namespaces', 'Namespace Members'] }],
      ['concepts', { Link: 'concepts.html', Path: ['Concepts'] }],
      ['classList', { Link: 'annotated.html', Path: ['Classes', 'Class List'] }],
      ['classMems', { Link: null, Path: ['Classes', 'Class Members'] }],
      ['classHierarchy', { Link: 'hierarchy.html', Path: ['Classes', 'Class Hierarchy'] }],
      ['classIndex', { Link: 'classes.html', Path: ['Classes', 'Class Index'] }],
      ['fileList', { Link: 'files.html', Path: ['Files', 'File List'] }],
      ['fileMems', { Link: null, Path: ['Files', 'File Members'] }],
      ['examples', { Link: 'examples.html', Path: ['Examples'] }],
      ['devHub', { Link: 'dp_dh_dir_dev_hub.html', Path: [] }],
      ['dhBugs', { Link: 'dp_dh_bugs.html', Path: [] }],
      ['dhRequired', { Link: 'dp_dh_required.html', Path: [] }],
      ['deprList', { Link: 'deprecated.html', Path: ['Deprecated List'] }],
      ['bugsList', { Link: 'bug.html', Path: ['Bug List'] }],
      ['todoList', { Link: 'todo.html', Path: ['Todo List'] }],
      ['testList', { Link: 'test.html', Path: ['Test List'] }],
    ]);

    // Traverse prunedNavTree and resolve matching pending entries.
    const found = (() => {

      // Result object that will be returned and assigned to found object.
      const obj = Object.create(null);

      // If the pending Map is empty, display a fatal error and return the empty object.
      if (pending.size === 0) {
        dispFatalError('initHtmlData(): The pending Map is empty, there is nothing to find in prunedNavTree.');
        return obj;
      }

      // Returns true if 'need' exactly matches the trailing part of 'have'.
      // In other words, 'need' must appear as a contiguous suffix of 'have'.
      // Examples:
      // - have = ['Z', 'A', 'B', 'C'], need = ['A', 'B', 'C'] -> true
      // - have = ['A', 'B', 'C'],      need = ['A', 'B', 'C'] -> true
      // - have = ['Z', 'Y', 'A', 'B', 'C'], need = ['A', 'B', 'C'] -> true
      // - have = ['Z', 'A', 'M', 'B', 'C'], need = ['A', 'B', 'C'] -> false
      // Here, 'have' is the currently available path and 'need' is the required
      // path suffix stored in the pending Map.
      function matchPath(have, need) {
        if (need.length === 0) return true;
        if (have.length < need.length) return false;
        const beg = have.length - need.length;
        for (let ii = 0; ii < need.length; ++ii) {
          if (have[ii + beg] !== need[ii]) return false;
        }
        return true;
      }

      // FIFO work queue used to traverse prunedNavTree and search for pending items.
      const queue = [];

      // Adds a prunedNavTree node to the queue.
      // 'path' contains the names of the node's ancestors.
      // The queue stores only the last href segment. For example, if a node href is
      // "A/B/c.html", the stored value is "c.html". This allows matching by HTML file
      // name only. Doxygen-generated HTML names already encode the ancestor context
      // (such as namespace and class), so matching the full path is unnecessary and
      // would only reflect the folder structure.
      function addToQueue(node, path = []) {
        const href = node[1] ? node[1].split('/').pop() : null;
        queue.push({ Node: node, Link: href, Path: path.concat(node[0]) });
      }

      // Add the top-level nodes of prunedNavTree to the queue.
      for (const node of prunedNavTree) addToQueue(node);

      // Current queue index acting as the FIFO head.
      let head = 0;

      // While the queue still has items and the pending Map is not empty,
      // try to match queued nodes against pending entries. Each node is expected
      // to satisfy at most one pending entry, so stop after the first match.
      while (head < queue.length && pending.size > 0) {
        const item = queue[head++];
        for (const [key, val] of pending) {
          if (matchPath(item.Path, val.Path) && (val.Link === null || val.Link === item.Link)) {
            obj[key] = item.Node;
            pending.delete(key);
            break;
          }
        }

        // Since prunedNavTree is already pruned and normalized, if node[2] exists 
        // then it has to be a non-empty branch.
        if (item.Node[2]) {
          for (const node of item.Node[2]) addToQueue(node, item.Path);
        }
      }

      return obj;
    })();

    // Remaining pending entries
    const htmlNotFound = [];
    if (pending.size > 0) {
      for (const [, val] of pending) htmlNotFound.push([val.Path.join(' > '), val.Link]);
    }

    // List of nodes whose hrefs are deliberately ignored.
    const ignoredList = [];
    const seenIgnoredHref = new Set();

    // Build and return a NEW branch for vars.htmlData from a branch of prunedNavTree.
    function convertBranch(branch, parentChain, ...regexList) {
      // Output node format:
      // - [label, null, kids] for branches
      // - [label, null, href] for leaves
      // Rules:
      // 1. Traverse the input branch recursively.
      // 2. Keep a branch only if at least one converted child remains.
      // 3. Keep each leaf href at most once per call.
      // 4. If regex filters are given, keep only leaves whose href base name
      //    (file name without path or extension) matches at least one regex.
      // 5. If a node has both href and kept children, keep it only as a branch
      //    and record its href in ignoredList once.
      // The parentChain is used only to build readable paths such as
      // "A > B > C" for ignored-link reporting.

      // Invalid or empty input branch produces an empty result branch.
      if (!Array.isArray(branch) || branch.length === 0) return [];

      // All extra arguments must be RegExp objects.
      // On failure, report a fatal error and return an empty branch.
      for (const regex of regexList) {
        if (!(regex instanceof RegExp)) {
          dispFatalError('initHtmlData() - convertBranch(): Invalid Regex found in regexList parameter', regex);
          return [];
        }
      }

      // Returns true if the href base name matches any regex in regexList.
      // Example: "a/b/class_foo.html" -> "class_foo".
      function testHrefBase(fullHref) {
        const baseHref = fullHref.split('/').pop();
        const dotPos = baseHref.lastIndexOf('.');
        const nameHref = (dotPos >= 0) ? baseHref.slice(0, dotPos) : baseHref;
        for (const regex of regexList) {
          // Reset lastIndex before each test for /g and /y regex safety.
          regex.lastIndex = 0;
          if (regex.test(nameHref)) return true;
        }
        return false;
      }

      // Tracks hrefs already emitted into the returned branch.
      // This prevents duplicate leaf entries within this call.
      const seen = new Set();

      // Recursively convert one sub-branch into the vars.htmlData format.
      // The curParChain is only used for ignored-link reporting.
      function convert(subBranch, curParChain) {
        if (!Array.isArray(subBranch) || subBranch.length === 0) return [];
        const rows = [];
        for (const node of subBranch) {

          // Human-readable full path for diagnostics / ignored-list reporting.
          // curParChain("A > B") + node[0] ("C") => name ("A > B > C")
          const name = curParChain ? `${curParChain} > ${node[0]}` : node[0];

          // Convert children first to decide whether this node survives as a branch.
          const kids = node[2] ? convert(node[2], name) : [];

          if (kids.length > 0) {
            // Branch case: keep children and ignore the node href in output.
            // Record that ignored href once if it exists.
            if (node[1] && !seen.has(node[1]) && !seenIgnoredHref.has(node[1])) {
              ignoredList.push([name, node[1]]);
              seenIgnoredHref.add(node[1]);
            }
            rows.push([node[0], null, kids]);
          }
          else if (node[1] && !seen.has(node[1])) {
            // Leaf case: keep only unique hrefs.
            // If filters exist, the href base name must match one of them.
            seen.add(node[1]);
            if (regexList.length === 0 || testHrefBase(node[1])) {
              rows.push([node[0], null, node[1]]);
            }
          }

          // Otherwise skip the node.
        }
        return rows;
      }

      // Convert the input branch and return the new vars.htmlData branch.
      return convert(branch, parentChain);
    }

    // Group a flat converted list into alphabetic buckets and return the NEW list.
    function putInAlphabeticBuckets(flatList) {
      // Input:
      // - flatList is expected to be a converted flat list where each item is a
      //   node in the form [label, null, hrefOrKids].
      // Output:
      // - Returns a NEW list of bucket nodes in the form [bucketLabel, null, kids].
      // Rules:
      // 1. Group items by the uppercased first character of node[0].
      // 2. If that character is between 'A' and 'Z', use it as the bucket name.
      // 3. All other starting characters go into the 'Other' bucket.
      // 4. Buckets are emitted in fixed order: 'A' to 'Z', then 'Other'.
      // 5. Empty or invalid input returns an empty list.

      // Invalid or empty input produces an empty result.
      if (!Array.isArray(flatList) || flatList.length === 0) return [];

      // Map each bucket name to the list of nodes that belong to it.
      const map = new Map();
      for (const node of flatList) {
        const firstChar = node[0][0].toUpperCase();
        const code = firstChar.charCodeAt(0);
        const key = (code >= 65 && code <= 90) ? firstChar : 'Other';
        if (!map.has(key)) map.set(key, []);
        map.get(key).push(node);
      }

      // Build the final bucketed output list.
      const out = [];

      // Emit alphabetic buckets in fixed A-Z order.
      for (let cc = 65; cc <= 90; ++cc) {
        const key = String.fromCharCode(cc);
        const kids = map.get(key);
        if (kids && kids.length > 0) {
          out.push([key, null, kids]);
        }
      }

      // Emit the non-alphabetic bucket last if it has any items.
      const otherKids = map.get('Other');
      if (otherKids && otherKids.length > 0) {
        out.push(['Other', null, otherKids]);
      }

      return out;
    }

    // The main top nodes for the doxy-plus root pane.
    const theProject = ['Project', null, []];
    const theDependencies = ['Dependencies', null, []];
    const theArchitecture = ['Architecture', null, []];
    const theNamespaces = ['Namespaces', null, []];
    const theConcepts = ['Concepts', null, []];
    const theClasses = ['Classes', null, []];
    const theFiles = ['Files', null, []];
    const theExamples = ['Examples', null, []];
    const theDevHub = ['Dev Hub', null, []];

    // Main 'index.html' page as overview page
    if (found.mainPage) theProject[2].push(['Overview', null, found.mainPage[1]]);

    // Doxygen Requirements Page
    if (found.requirements) theProject[2].push([found.requirements[0], null, found.requirements[1]]);

    // Dependency Required Page
    if (found.depRequired) theDependencies[2].push([found.depRequired[0], null, found.depRequired[1]]);

    // Dependencies
    if (found.dependencies) {
      seenIgnoredHref.add(found.dependencies[1]);
      ignoredList.push([found.dependencies[0], found.dependencies[1]]);
      if (theDependencies[2].length === 0) {
        theDependencies[2] = convertBranch(found.dependencies[2], 'Dependencies', /^dp_dep_/);
      }
      else {
        const tempDepList = convertBranch(found.dependencies[2], 'Dependencies', /^dp_dep_/);
        theDependencies[2].push(...tempDepList);
      }
    }

    // Architecture
    if (found.architecture) {
      seenIgnoredHref.add(found.architecture[1]);
      ignoredList.push([found.architecture[0], found.architecture[1]]);
      theArchitecture[2] = convertBranch(found.architecture[2], 'Architecture', /^dp_arch_/);
    }

    // Namespace List
    if (found.namespaceList) {

      // Add main page to project node
      theProject[2].push(['Namespace List', null, found.namespaceList[1]]);

      // Populate
      (function processNamespaceListBranch(branch, path = null) {
        if (!Array.isArray(branch) || branch.length === 0) return;

        // Separate normal and anonymous namespace first, on same
        // level anonymous namespace always goes below normal ones.
        const nsNormList = [];
        const nsAnonList = [];
        for (const node of branch) {
          const href = node[1] ? node[1].split('/').pop() : null;
          if (href && /^namespace/.test(href)) {
            const pName = prettierAnon(node[0]);
            if (pName === node[0]) nsNormList.push([pName, node[1], node[2]]);
            else nsAnonList.push([pName, node[1], node[2]]);
          }
        }

        // Push normal namespace to namespace list branch.
        for (const node of nsNormList) {
          theNamespaces[2].push([node[0], path, node[1]]);
          if (node[2]) {
            const childPath = path ? `${path}::${node[0]}` : node[0];
            processNamespaceListBranch(node[2], childPath);
          }
        }

        // Push anonymous namespace to namespace list branch.
        for (const node of nsAnonList) {
          theNamespaces[2].push([node[0], path, node[1]]);
          if (node[2]) {
            const childPath = path ? `${path}::${node[0]}` : node[0];
            processNamespaceListBranch(node[2], childPath);
          }
        }

      })(found.namespaceList[2]);
    }

    // Namespace Members
    if (found.namespaceMems && found.namespaceMems[2]) {
      const converted = convertBranch(found.namespaceMems[2], 'Namespaces > Namespace Members');
      if (converted.length > 0) theNamespaces[2].push(['[Members]', null, converted]);
    }

    // Concepts
    if (found.concepts) {

      // Add main page to project node
      theProject[2].push(['Concepts List', null, found.concepts[1]]);

      // Populate
      (function processConceptsBranch(branch, prefix = null) {
        if (!Array.isArray(branch) || branch.length === 0) return;
        for (const node of branch) {
          const pName = prettierAnon(node[0]);

          const href = node[1] ? node[1].split('/').pop() : null;
          if (href && /^concept/.test(href)) {
            theConcepts[2].push([pName, prefix, node[1]]);
          }

          if (node[2]) {
            const childPrefix = prefix ? `${prefix}::${pName}` : pName;
            processConceptsBranch(node[2], childPrefix);
          }
        }
      })(found.concepts[2]);

      // Sort
      theConcepts[2].sort((a, b) => {

        // 1. Concept name.
        let cmpResult = a[0].localeCompare(b[0], undefined, { sensitivity: 'base' });
        if (cmpResult !== 0) return cmpResult;

        // 2. Path, but keep alphabetic namespaces before <file.cpp>-style ones.
        cmpResult = compareAlphaBeforePunctuation(a[1] || '', b[1] || '');
        if (cmpResult !== 0) return cmpResult;

        // 3. Final tie-breaker.
        return a[2].localeCompare(b[2], undefined, { sensitivity: 'base' });
      });

      // Change to alphabetic buckets if needed
      if (theConcepts[2].length > CONSTS.VAL.BUCKET_THRESHOLD) {
        theConcepts[2] = putInAlphabeticBuckets(theConcepts[2]);
      }
    }

    // Classes
    if (found.classList) {

      // Add main page to project node
      theProject[2].push(['Class List', null, found.classList[1]]);

      // Populate
      (function processClassListBranch(branch, namespacePath = null, classPath = null) {
        if (!Array.isArray(branch) || branch.length === 0) return;
        for (const node of branch) {

          let namespaceName = namespacePath;
          let className = classPath;

          const href = node[1] ? node[1].split('/').pop() : null;
          if (href) {
            const pName = prettierAnon(node[0]);
            if (/^(?:class|struct)/.test(href)) {
              className = classPath ? `${classPath}::${pName}` : pName;
              theClasses[2].push([className, namespaceName, node[1]]);
            }
            else if (/^namespace/.test(href)) {
              namespaceName = namespacePath ? `${namespacePath}::${pName}` : pName;
            }
          }

          if (node[2]) processClassListBranch(node[2], namespaceName, className);
        }
      })(found.classList[2]);

      // Sort
      theClasses[2].sort((a, b) => {
        // split class names
        const aClasses = a[0].split('::');
        const bClasses = b[0].split('::');

        // 1. Outermost class name.
        let cmpResult = aClasses[0].localeCompare(bClasses[0], undefined, { sensitivity: 'base' });
        if (cmpResult !== 0) return cmpResult;

        // 2. Namespace path, but keep alphabetic namespaces before <file.cpp>-style ones.
        cmpResult = compareAlphaBeforePunctuation(a[1] || '', b[1] || '');
        if (cmpResult !== 0) return cmpResult;

        // 3. Inner class segments.
        const minLen = Math.min(aClasses.length, bClasses.length);
        for (let ii = 1; ii < minLen; ++ii) {
          cmpResult = aClasses[ii].localeCompare(bClasses[ii], undefined, { sensitivity: 'base' });
          if (cmpResult !== 0) return cmpResult;
        }

        // 4. Shorter path first if one is a prefix of the other.
        cmpResult = aClasses.length - bClasses.length;
        if (cmpResult !== 0) return cmpResult;

        // 5. Final tie-breaker.
        return a[2].localeCompare(b[2], undefined, { sensitivity: 'base' });
      });

      // Change to alphabetic buckets if needed
      if (theClasses[2].length > CONSTS.VAL.BUCKET_THRESHOLD) {
        theClasses[2] = putInAlphabeticBuckets(theClasses[2]);
      }
    }

    // Class Members
    if (found.classMems && found.classMems[2]) {
      const converted = convertBranch(found.classMems[2], 'Classes > Class Members');
      if (converted.length > 0) theClasses[2].push(['[Members]', null, converted]);
    }

    // Class Hierarchy
    if (found.classHierarchy) theClasses[2].push(['[Hierarchy]', null, found.classHierarchy[1]]);

    // Class Index
    if (found.classIndex) theClasses[2].push(['[Index]', null, found.classIndex[1]]);

    // File List
    if (found.fileList) {
      theProject[2].push(['File List', null, found.fileList[1]]);
      theFiles[2] = convertBranch(found.fileList[2], 'Files > File List', /_8(hpp|h|cpp|c)$/);
    }

    // File Members
    if (found.fileMems && found.fileMems[2]) {
      const converted = convertBranch(found.fileMems[2], 'Files > File Members');
      if (converted.length > 0) theFiles[2].push(['[Members]', null, converted]);
    }

    // Example List
    if (found.examples) {
      theProject[2].push(['Examples List', null, found.examples[1]]);
      theExamples[2] = convertBranch(found.examples[2], 'Examples', /-example$/);
    }

    // Dev Hub
    if (found.devHub) {
      seenIgnoredHref.add(found.devHub[1]);
      ignoredList.push([found.devHub[0], found.devHub[1]]);
      theDevHub[2] = convertBranch(found.devHub[2], 'Dev Hub', /^dp_dh_/);
    }

    // Dev Hub Bugs Page
    if (found.dhBugs) {
      // splice(1, 0, 'bla') inserts 'bla' at position 1, 0 = removes nothing 
      if (theDevHub[2].length > 0) theDevHub[2].splice(1, 0, [found.dhBugs[0], null, found.dhBugs[1]]);
      else theDevHub[2].push([found.dhBugs[0], null, found.dhBugs[1]]);
    }

    // Dev Hub Required Page
    if (found.dhRequired) {
      // splice(1, 0, 'bla') inserts 'bla' at position 1, 0 = removes nothing 
      if (theDevHub[2].length > 0) theDevHub[2].splice(1, 0, [found.dhRequired[0], null, found.dhRequired[1]]);
      else theDevHub[2].push([found.dhRequired[0], null, found.dhRequired[1]]);
    }

    // Deprecated List
    if (found.deprList) theProject[2].push(['Deprecated List', null, found.deprList[1]]);

    // Bug List
    if (found.bugsList) theProject[2].push(['Bug List', null, found.bugsList[1]]);

    // Todo List
    if (found.todoList) theProject[2].push(['Todo List', null, found.todoList[1]]);

    // Test List
    if (found.testList) theProject[2].push(['Test List', null, found.testList[1]]);

    // Assigning to vars.htmlData array
    if (theProject[2].length > 0) vars.htmlData.push(theProject);
    if (theDependencies[2].length > 0) vars.htmlData.push(theDependencies);
    if (theArchitecture[2].length > 0) vars.htmlData.push(theArchitecture);
    if (theNamespaces[2].length > 0) vars.htmlData.push(theNamespaces);
    if (theConcepts[2].length > 0) vars.htmlData.push(theConcepts);
    if (theClasses[2].length > 0) vars.htmlData.push(theClasses);
    if (theFiles[2].length > 0) vars.htmlData.push(theFiles);
    if (theExamples[2].length > 0) vars.htmlData.push(theExamples);
    if (theDevHub[2].length > 0) vars.htmlData.push(theDevHub);

    // Check generated data
    checkTreeData(vars.htmlData, 'html', true);

    // Save HTML data and ignored list
    storage.htmlData.save(vars.htmlData);
    storage.htmlIgnored.save(ignoredList);

    // Save not found list if it is non-empty
    if (htmlNotFound.length > 0) storage.htmlNotFound.save(htmlNotFound);
    else storage.htmlNotFound.remove();

    // Check for redirection, if true then return
    if (checkRedirect(vars.htmlData)) return;

    // Save the current page as prevHtml for future use
    storage.htmlPrevName.save(CONSTS.HTML_NAME);

    // Assign dev hub class for detection in CSS
    if (vars.htmlData.length > 0 && vars.htmlData[vars.htmlData.length - 1][0] === 'Dev Hub') {
      document.body.classList.add(CONSTS.CLS.DEV_HUB_AVAILABLE);
    }

    // Deep freeze vars.htmlData
    deepFreeze(vars.htmlData);
  }

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // #endregion 🟥 INIT HTML DATA

  // #region 🟩 POPULATE ELEMENTS
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  // Find elements from CONSTS.SEL and store them in vars.els
  function populateElements() {
    Object.keys(vars.els).forEach((key) => { delete vars.els[key]; });
    Object.assign(
      vars.els,
      Object.fromEntries(
        Object.entries(CONSTS.SEL).map(([key, selector]) => [key, document.querySelector(selector)])
      )
    );
  }

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // #endregion 🟥 POPULATE ELEMENTS

  // #region 🟩 INIT HASH DATA
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  // Initialize vars.hashData array
  async function initHashData() {

    // Reset the hash data before rebuilding it, while preserving its reference
    vars.hashData.length = 0;

    // Load the stored hash data for this page
    const prevHashData = storage.hashData.load();

    // If stored hash data is explicitly empty then return
    if (prevHashData === 'Empty') return;

    // If stored hash data is a non-empty array then assign and return
    if (Array.isArray(prevHashData) && prevHashData.length > 0) {

      // Check stored data
      if (checkTreeData(prevHashData, 'hash')) {
        document.body.classList.add(CONSTS.CLS.HASH_DATA_AVAILABLE);
        const isCode = storage.hashDataIsCode.load();
        if (isCode === 'yes') {
          document.body.classList.add(CONSTS.CLS.HASH_DATA_IS_CODE);
        }
        else {
          const hasDoxsections = storage.pageHasDoxsections.load();
          if (hasDoxsections === 'yes') {
            vars.pageHasDoxsections = true;
          }
        }
        vars.hashData.push(...prevHashData);
        deepFreeze(vars.hashData);
        return;
      }
      else {
        storage.hashData.remove();
        storage.hashDataIsCode.remove();
        storage.pageHasDoxsections.remove();
      }
    }

    // Ensure vars.els.PAGE_CONTENT exists and is still connected before generating hash data.
    if (!vars.els.PAGE_CONTENT || !vars.els.PAGE_CONTENT.isConnected) {
      vars.els.PAGE_CONTENT = await waitFor(CONSTS.SEL.PAGE_CONTENT);
      if (!vars.els.PAGE_CONTENT) {
        console.warn(`initHashData(): Selector ${CONSTS.SEL.PAGE_CONTENT} not found; hash data could not be generated.`);
        return;
      }
    }

    // Formats signatures for C++ types
    function formatSignature(text) {
      if (typeof text !== 'string') return text;

      return text
        // Remove space before *, &, &&
        .replace(/\s+([*&]{1,2})/g, '$1')

        // Ensure space after *, &, &&
        .replace(/([*&]{1,2})(?!\s)/g, '$1 ')

        // Remove spaces inside <...>
        .replace(/<\s+/g, '<')
        .replace(/\s+>/g, '>')
        .replace(/\s+<\s+/g, '<')
        .replace(/\s+>\s+/g, '>')

        // Remove space before commas, ensure one after
        .replace(/\s+,/g, ',')
        .replace(/,(?!\s)/g, ', ')

        // Remove space after ( and before )
        .replace(/\(\s+/g, '(')
        .replace(/\s+\)/g, ')')

        // Remove space before (
        .replace(/\s+\(/g, '(')

        // Remove space before {
        .replace(/\s+\{/g, '{')

        // Add space before and after = in special cases
        .replace(/\s*=\s*(default|delete|0)/g, ' = $1')

        // Collapse multiple spaces and trim
        .replace(/\s{2,}/g, ' ')

        // leading "}"
        .replace(/^\}+\s*/, '')

        // trailing "{"
        .replace(/\s*\{+$/, '')

        .trim();
    }

    // Collect member signatures and group them into vars.hashData by section header.
    // --- NOT for markdown pages ---
    // The '<tbody>' of '<table class="memberdecls">' inside '<div class=contents">'
    // constains the list of members. This list of members usually looks like
    // '<tr class="memitem:abc" id="r_abc">' and this is what is use as 'row' in
    // this function.
    // The tables below comes from <body>...<div id="container">...<div id="doc-content">
    // ...<div class="contents">...<table class="memberdecls">. The <table class="memberdecls">
    // has <tbody> which has 
    // - one <tr class="heading"> which contains headings for the group (e.g. "Public Member 
    //   Functions", "Private Types", etc.). From here we get the heading for the section. And,
    // - one or more <tr class="memitem:..." id="r_..."> which contains the each member links 
    //   and text. Here we get the links. Each <tr class="memitem:..." id="r_..."> contains
    //   <td class="memItemLeft"...> and <td class="memItemRight"...>. Here:
    //   - <td class="memItemLeft"...> might contain links but these are not the correct links.
    //     This class provides text for the type of a function or variable (e.g. "void", "static
    //     bool", etc.) as its innerText. It can be empty.
    //   - <td class="memItemRight"...> can have one or more <a class="el" href="#abcdefg">. The
    //     first <a class="el" href="#abcdefg"> contains the real hash in href="#abcdefg" for the
    //     member as well as the text (function/variable name with parameters) for the member.
    const tables = Array.from(vars.els.PAGE_CONTENT.querySelectorAll("table.memberdecls"));
    tables.forEach((table, idx) => {
      const grpSigs = [];
      const seenHref = new Set();

      const headerEl = table.querySelector("h2.groupheader");
      if (!headerEl) return;

      let headName = (headerEl.textContent || '').replace(/\s+/g, ' ').trim();
      if (headName.length === 0) headName = `Members ${idx + 1}`;
      headName = prettierAnon(headName);

      const rows = table.querySelectorAll('tr');
      rows.forEach((row) => {
        const lftTd = row.querySelector(':scope > td.memItemLeft');
        const ritTd = row.querySelector(':scope > td.memItemRight');

        if (!ritTd) return;

        const leafHref = ritTd.querySelector(':scope > a.el[href^="#"]')?.getAttribute('href') || '';
        if (leafHref.length === 0 || seenHref.has(leafHref)) return;

        let lftText = lftTd ? lftTd.innerText.replace(/\s+/g, ' ').trim() : '';
        let ritText = ritTd.innerText.replace(/\s+/g, ' ').trim();

        lftText = formatSignature(lftText);
        ritText = formatSignature(ritText);

        if (lftText.startsWith('enum')) {
          ritText = ritText.replace(/\s*\{[\s\S]*\}/, '').trim();
        }

        lftText = prettierAnon(lftText);
        ritText = prettierAnon(ritText);

        if (lftText.trim().length === 0) lftText = null;

        seenHref.add(leafHref);
        grpSigs.push([ritText, lftText, leafHref]);
      });

      // Add this group to hash data if it has entries
      if (grpSigs.length > 0) {
        vars.hashData.push([headName, null, grpSigs]);
      }
    });

    // The hash data was generated using member table, this means
    // we are on a page with code. Set the correct class lists,
    // save the data, freeze vars.hashData and return.
    if (vars.hashData.length > 0) {
      checkTreeData(vars.hashData, 'hash', true);
      document.body.classList.add(CONSTS.CLS.HASH_DATA_AVAILABLE);
      document.body.classList.add(CONSTS.CLS.HASH_DATA_IS_CODE);
      storage.hashData.save(vars.hashData);
      storage.hashDataIsCode.save('yes');
      deepFreeze(vars.hashData);
      return;
    }

    // Return the numeric heading level for a Doxygen Markdown section heading.
    function getDoxSectionLevel(el) {
      if (!el || typeof el.tagName !== 'string') return 0;
      const m = /^H([1-6])$/i.exec(el.tagName);
      return m ? Number(m[1]) : 0;
    }

    // Return the anchor id for a Doxygen Markdown section heading.
    function getDoxSectionAnchorId(el) {
      if (!el) return '';

      // Normal case:
      // <h2 class="doxsection"><a class="anchor" id="..."></a>Title</h2>
      const a = el.querySelector(':scope > a.anchor[id], :scope > a[id]');
      if (a && a.id) return a.id;

      // Fallback: id on the heading itself.
      if (el.id) return el.id;

      // Fallback: anchor immediately before the heading.
      const prev = el.previousElementSibling;
      if (prev && prev.matches && prev.matches('a.anchor[id], a[id]') && prev.id) {
        return prev.id;
      }

      return '';
    }

    // Returns true if a parent doxsection has content before first child doxsection
    function hasOwnContentBeforeNextDoxsection(sec) {
      let el = sec.nextElementSibling;

      while (el) {
        if (el.matches(':is(h1, h2, h3, h4, h5, h6).doxsection')) return false;
        if (el.tagName !== 'HR') return true;
        el = el.nextElementSibling;
      }

      return false;
    }

    // Get on-page heading anchors for Markdown pages.
    const doxSections = Array.from(vars.els.PAGE_CONTENT.querySelectorAll('.doxsection'));
    const doxSeenHref = new Set();
    const doxStack = []; // { level: number, node: [name, href|kids] }

    for (const sec of doxSections) {
      const level = getDoxSectionLevel(sec);
      if (level === 0) continue;

      const id = getDoxSectionAnchorId(sec);
      if (!id) continue;

      let name = (sec.textContent || '').replace(/\s+/g, ' ').trim();
      if (name.length === 0) continue;
      name = prettierAnon(name);

      const href = `#${id}`;
      if (doxSeenHref.has(href)) continue;
      doxSeenHref.add(href);

      const node = [name, null, href];

      while (doxStack.length > 0 && doxStack[doxStack.length - 1].level >= level) {
        doxStack.pop();
      }

      if (doxStack.length === 0) {
        vars.hashData.push(node);
      }
      else {
        const parentEntry = doxStack[doxStack.length - 1];
        const parent = parentEntry.node;
        if (!Array.isArray(parent[2])) {
          const parentHref = parent[2];
          parent[2] = [];
          if (hasOwnContentBeforeNextDoxsection(parentEntry.sec)) {
            parent[2].push(['Overview', null, parentHref]);
          }
        }
        parent[2].push(node);
      }

      doxStack.push({ level, node, sec });
    }

    // Hash data was generated using the 'doxsection' method for
    // markdown pages. In this case we do not set the class and
    // data for 'hashDataIsCode'. Just save the class list and
    // data for hash data, freeze vars.hashData and return.
    if (vars.hashData.length > 0) {
      checkTreeData(vars.hashData, 'hash', true);
      storage.pageHasDoxsections.save('yes');
      vars.pageHasDoxsections = true;
      document.body.classList.add(CONSTS.CLS.HASH_DATA_AVAILABLE);
      storage.hashData.save(vars.hashData);
      deepFreeze(vars.hashData);
      return;
    }

    // If we are here this means vars.hashData is still empty.
    // We store the empty sentinel explicitly for hash data 
    // so that next reload of this page does not try to regenrate 
    // the hash data and arrive at the same conclusion.
    storage.hashData.save('Empty');
  }

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // #endregion 🟥 INIT HASH DATA

  // #region 🟩 BUILD TREES
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  // Build <ul>...<li> trees from vars.htmlData and vars.hashData
  function buildTrees() {

    // Class list
    const CLS = {
      list: 'dpt-list',      // ul
      node: 'dpt-node',      // li, tree node
      row: 'dpt-row',        // clickable row (<button> for branch, <a> for leaf)
      toggle: 'dpt-toggle',  // branch toggle icon
      prefix: 'dpt-prefix',  // prefix, e.g. spe::
      label: 'dpt-label',    // visible node text
      sep: 'dpt-sep',        // Separator i.e. span

      leaf: 'dpt-node--leaf',         // Node with no children
      branch: 'dpt-node--branch',     // Node with children
      current: 'dpt-node--current',   // Current node
      visited: 'dpt-node--visited',   // Visited node (only for hashTree)
      expanded: 'dpt-node--expanded', // Expanded branch node
    };

    // Map from hash href to its constructed leaf node.
    const hashToNode = new Map();

    // Handle only expand/collapse clicks; links work automatically.
    function clickHandler(evt) {

      // Find the target
      const target = evt.target instanceof Element ? evt.target : null;
      if (!target) return;

      // Return unless the clicked row is a branch button.
      const row = target.closest(`.${CLS.row}`);
      if (!(row instanceof HTMLButtonElement)) return;

      // Find the node
      const li = row.closest(`.${CLS.node}`);
      if (!li || !li.classList.contains(CLS.branch)) return;

      // Toggle its expanded state
      const isExpanded = li.classList.toggle(CLS.expanded);
      row.setAttribute('aria-expanded', isExpanded ? 'true' : 'false');
    }

    // Mark a node as current and expand its parent chain.
    function markAndExpandCurrentNode(curNode) {
      if (!curNode) return;
      curNode.classList.add(CLS.current);
      let curPar = curNode.parentElement ? curNode.parentElement.closest(`.${CLS.node}`) : null;
      while (curPar) {
        if (curPar.classList.contains(CLS.branch)) {
          curPar.classList.add(CLS.expanded);
          const row = curPar.firstElementChild;
          if (row instanceof HTMLButtonElement) row.setAttribute('aria-expanded', 'true');
        }
        curPar = curPar.parentElement ? curPar.parentElement.closest(`.${CLS.node}`) : null;
      }
    }

    // Handle on-page hash changes.
    function onHashChange() {

      // Find previous current node first
      const prev = vars.els.DP_LEAF_SHOW.querySelector(`.${CLS.node}.${CLS.current}`);

      // Read the current hash
      const hash = window.location.hash;

      // If hash is empty then clear previous current and stop
      if (!hash) {
        if (prev) {
          prev.classList.remove(CLS.current);
          prev.classList.add(CLS.visited);
        }
        return;
      }

      // Find node directly from map
      const item = hashToNode.get(hash) ?? null;

      // Clear previous item only if it is different from the new one
      if (prev && prev !== item) {
        prev.classList.remove(CLS.current);
        prev.classList.add(CLS.visited);
      }

      // Return if new item is not found
      if (!item) return;

      // Clear visited state from the new item
      item.classList.remove(CLS.visited);

      // Mark it current and expand its ancestor path
      markAndExpandCurrentNode(item);

      // Scroll into view
      item.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
    }

    // Build a tree from the given data and config.
    function buildTreeFromData(data, cfg) {
      if (!Array.isArray(data) || data.length === 0) return null;

      let curNode = null;

      // Create root <ul>.
      const root = document.createElement('ul');
      root.classList.add(CLS.list);

      // Stack used for iterative tree construction.
      const stack = [{ branch: data, parent: root }];
      while (stack.length) {

        const { branch, parent } = stack.pop();
        if (!Array.isArray(branch) || branch.length === 0) continue;

        const fragment = document.createDocumentFragment();

        for (let ii = 0; ii < branch.length; ++ii) {

          const item = branch[ii];

          // li for the node
          const li = document.createElement('li');
          li.classList.add(CLS.node);

          // Obtain prefix text (namespace/scope) and value (href or kids)
          const prefixText = cfg.getPrefixText(item);
          const value = cfg.getValue(item);

          // Create prefix as span if prefix text exists
          let prefix = null;
          if (prefixText) {
            prefix = document.createElement('span');
            prefix.classList.add(CLS.prefix);
            prefix.textContent = prefixText;
          }

          // If value is of type string then this is a leaf node
          if (typeof value === 'string') {

            // Mark li as leaf
            li.classList.add(CLS.leaf);

            // Create row as a link
            const row = document.createElement('a');
            row.classList.add(CLS.row);
            row.href = cfg.getHref(item);
            row.title = cfg.getTitle(item);

            // Append prefix and labels to row
            if (prefix) row.append(prefix);
            const labels = cfg.getLabelSpans(item);
            for (const lbl of labels) row.append(lbl);

            // Append row to li
            li.appendChild(row);

            // Run the optional leaf callback.
            if (cfg.onLeaf) cfg.onLeaf(row, li);

            // Check for the node being the current node
            if (cfg.isCurrent(row)) curNode = li;
          }
          else if (Array.isArray(value) && value.length > 0) {
            // Value is non-empty array i.e. a branch

            // Mark li as a branch
            li.classList.add(CLS.branch);

            // Create row as a button for expand/collapse
            const row = document.createElement('button');
            row.classList.add(CLS.row);
            row.setAttribute('type', 'button');
            row.setAttribute('aria-expanded', 'false');
            row.title = cfg.getTitle(item);

            // Add toggle as span to show expand/collapse icon
            const toggle = document.createElement('span');
            toggle.classList.add(CLS.toggle);
            toggle.textContent = "\u2B9E"; // ⮞, rotated to show open state
            toggle.setAttribute('aria-hidden', 'true');

            // Append toggle, prefix and labels to row
            row.append(toggle);
            if (prefix) row.append(prefix);
            const labels = cfg.getLabelSpans(item);
            for (const lbl of labels) row.append(lbl);

            // Append row to li
            li.appendChild(row);

            // Create a ul sub-branch
            const twig = document.createElement('ul');
            twig.classList.add(CLS.list);
            li.appendChild(twig);

            // Queue the child branch and its parent <ul> for later processing.
            stack.push({ branch: value, parent: twig });
          }
          else {
            // Warn if the node is neither a leaf nor a branch. This should never happen.
            console.warn(`${cfg.warnName}: The following node is neither a leaf nor a branch.`, item);
            continue;
          }

          // Append the item <li> to its parent list <ul> fragment
          fragment.appendChild(li);
        }

        // Append fragment to parent <ul>
        parent.appendChild(fragment);
      }

      // Mark and expand the current node
      markAndExpandCurrentNode(curNode);

      // Return built tree
      return root;
    }

    function normalizePageHref(href) {
      let out = href.split('#')[0];
      if (out.endsWith('/')) out += 'index.html';
      return out;
    }

    const curHref = normalizePageHref(window.location.href);
    const curHash = window.location.hash;

    // Tree built from vars.htmlData
    const htmlTree = buildTreeFromData(vars.htmlData, {
      warnName: 'buildTreeFromData(): vars.htmlData',
      getLabelSpans: (item) => {
        const labels = [];
        const labelParts = String(item[0]).split('::');
        for (const lblPart of labelParts) {
          if (labels.length > 0) {
            const sep = document.createElement('span');
            sep.classList.add(CLS.sep);
            sep.textContent = '::';
            labels.push(sep);
          }
          const label = document.createElement('span');
          label.classList.add(CLS.label);
          label.textContent = lblPart;
          labels.push(label);
        }
        return labels;
      },
      getPrefixText: (item) => item[1] ? `${item[1]}::` : null,
      getValue: (item) => item[2],
      getHref: (item) => CONSTS.DOC_ROOT + item[2],
      getTitle: (item) => item[1] ? `${item[1]}::${item[0]}` : item[0],
      isCurrent: (row) => normalizePageHref(row.href) === curHref,
    });

    // Tree built from vars.hashData
    const hashTree = buildTreeFromData(vars.hashData, {
      warnName: 'buildTreeFromData(): vars.hashData',
      getLabelSpans: (item) => {
        const labels = [];
        const label = document.createElement('span');
        label.classList.add(CLS.label);
        label.textContent = item[0];
        labels.push(label);
        return labels;
      },
      getPrefixText: (item) => item[1] ? `${item[1]}\u00A0` : null,
      getValue: (item) => item[2],
      getHref: (item) => item[2],
      getTitle: (item) => item[1] ? `${item[1]} ${item[0]}` : item[0],
      isCurrent: (row) => row.getAttribute('href') === curHash,
      onLeaf: (row, li) => {
        const hash = row.getAttribute('href');
        if (hash) hashToNode.set(hash, li);
      },
    });

    // Mount htmlTree
    vars.els.DP_ROOT_PANE.replaceChildren();
    if (htmlTree) {
      vars.els.DP_ROOT_PANE.appendChild(htmlTree);
      htmlTree.addEventListener('click', clickHandler);
    }

    // Mount hashTree
    vars.els.DP_LEAF_SHOW.replaceChildren();
    if (hashTree) {
      vars.els.DP_LEAF_SHOW.appendChild(hashTree);
      hashTree.addEventListener('click', clickHandler);
      window.addEventListener('hashchange', onHashChange);
    }
  }

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // #endregion 🟥 BUILD TREES

  // #region 🟩 INIT DUAL PANE
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  // Initialize the root and leaf panes and wire up their resizing behavior.
  function initDualPane() {

    // Timer ID for scheduling a save of both pane widths.
    let paneWidthSaveTimer = null;

    // Update and apply the root pane width.
    function setRootPaneWidth(w) {
      if (w !== vars.rootPaneWidth) {
        vars.rootPaneWidth = w;
        document.documentElement.style.setProperty(CONSTS.PROP.ROOT_PANE_WIDTH, `${w}px`);
      }
    }

    // Update and apply the leaf pane width.
    function setLeafPaneWidth(w) {
      if (w !== vars.leafPaneWidth) {
        vars.leafPaneWidth = w;
        document.documentElement.style.setProperty(CONSTS.PROP.LEAF_PANE_WIDTH, `${w}px`);
      }
    }

    // Return whether the leaf pane should be shown.
    function isLeafPaneActive() {
      return vars.hashData.length > 0
        && vars.showLeafPane
        && (!vars.autoHideLists || window.innerWidth > CONSTS.VAL.LEAF_PANE_AUTO_HIDE_THRESHOLD);
    }

    // Adjust pane widths on window resize in desktop view.
    function checkDualPaneLayout() {

      // Return if not in desktop view
      if (window.innerWidth < CONSTS.VAL.DESKTOP_VIEW_MIN_WIDTH) return;

      // Proceed only if the combined width exceeds the maximum allowed width.
      const maxAllowed = window.innerWidth - CONSTS.VAL.GUTTER_W;
      const combinedWidth = vars.rootPaneWidth + vars.leafPaneWidth;
      if (combinedWidth > maxAllowed) {

        // Calculate rebalanced widths.
        let nRoot = isLeafPaneActive() ?
          Math.floor(maxAllowed * (vars.rootPaneWidth / combinedWidth))
          : Math.min(vars.rootPaneWidth, maxAllowed - CONSTS.VAL.MIN_W);
        let nLeaf = maxAllowed - nRoot;

        // Enforce the minimum width for both panes.
        if (nRoot < CONSTS.VAL.MIN_W) {
          nLeaf = Math.max(CONSTS.VAL.MIN_W, maxAllowed - CONSTS.VAL.MIN_W);
          nRoot = CONSTS.VAL.MIN_W;
        }
        else if (nLeaf < CONSTS.VAL.MIN_W) {
          nRoot = Math.max(CONSTS.VAL.MIN_W, maxAllowed - CONSTS.VAL.MIN_W);
          nLeaf = CONSTS.VAL.MIN_W;
        }

        // Apply the rebalanced widths.
        setRootPaneWidth(nRoot);
        setLeafPaneWidth(nLeaf);

        // Schedule saving both pane widths.
        clearTimeout(paneWidthSaveTimer);
        paneWidthSaveTimer = setTimeout(() => {
          storage.rootPaneWidth.save(vars.rootPaneWidth);
          storage.leafPaneWidth.save(vars.leafPaneWidth);
          paneWidthSaveTimer = null;
        }, CONSTS.VAL.PANE_WIDTH_SAVE_DEBOUNCE_MS);
      }
    }

    // Wire drag-resize behavior for either the root or leaf pane.
    function wirePaneResizer(isRootResizer) {

      // State for the current drag session.
      let raf = null;
      let startX = 0;
      let startW = 0;
      let movBeg = 0;

      // Starting widths used to detect changes on pointerup.
      let prevRootWidth = vars.rootPaneWidth;
      let prevLeafWidth = vars.leafPaneWidth;

      // Apply the latest dragged width.
      function applyWidth() {
        const newW = startW + (movBeg - startX);
        let maxAllowed = window.innerWidth - CONSTS.VAL.GUTTER_W;
        if (isRootResizer) {
          // Adjust root pane width
          if (isLeafPaneActive()) {
            // If leaf pane is actively being shown then we cannot shrink its
            // size to let root pane width grow.
            maxAllowed -= vars.leafPaneWidth;
            const nVal = Math.max(CONSTS.VAL.MIN_W, Math.min(maxAllowed, newW));
            setRootPaneWidth(nVal);
          }
          else {
            // If leaf pane is not actively being shown then we can shrink its
            // size to let root pane width grow.
            maxAllowed -= CONSTS.VAL.MIN_W;
            const nVal = Math.max(CONSTS.VAL.MIN_W, Math.min(maxAllowed, newW));
            setRootPaneWidth(nVal);
            if (nVal + vars.leafPaneWidth > window.innerWidth - CONSTS.VAL.GUTTER_W) {
              setLeafPaneWidth(window.innerWidth - CONSTS.VAL.GUTTER_W - nVal);
            }
          }
        }
        else {
          // Adjust leaf pane width
          maxAllowed -= vars.rootPaneWidth;
          const nVal = Math.max(CONSTS.VAL.MIN_W, Math.min(maxAllowed, newW));
          setLeafPaneWidth(nVal);
        }

        // Mark that no frame is currently pending.
        raf = null;
      }

      // Handle pointer movement during drag.
      function onMove(evt) {
        evt.preventDefault();
        movBeg = evt.clientX;
        if (raf) return; // One queued frame is enough.
        raf = requestAnimationFrame(applyWidth);
      }

      // Finish dragging and apply the last pending width if needed.
      function onUp(evt) {
        evt.preventDefault();

        // Flush the final width if a frame is still pending.
        if (raf) {
          cancelAnimationFrame(raf);
          applyWidth();
        }

        // Save widths if they changed during this drag session.
        if (prevRootWidth !== vars.rootPaneWidth) storage.rootPaneWidth.save(vars.rootPaneWidth);
        if (prevLeafWidth !== vars.leafPaneWidth) storage.leafPaneWidth.save(vars.leafPaneWidth);

        document.body.style.cursor = '';
        document.removeEventListener('pointermove', onMove);
        document.removeEventListener('pointerup', onUp);
        document.removeEventListener('pointercancel', onUp);
      }

      const resizer = isRootResizer ? vars.els.DP_ROOT_RESIZER : vars.els.DP_LEAF_RESIZER;
      const getW = isRootResizer ? () => vars.rootPaneWidth : () => vars.leafPaneWidth;

      // Start dragging when the resizer is pressed.
      resizer.addEventListener('pointerdown', (evt) => {
        evt.preventDefault();
        startX = evt.clientX;
        startW = getW();
        prevLeafWidth = vars.leafPaneWidth;
        prevRootWidth = vars.rootPaneWidth;
        document.body.style.cursor = 'ew-resize';
        document.addEventListener('pointermove', onMove, { passive: false });
        document.addEventListener('pointerup', onUp, { passive: false });
        document.addEventListener('pointercancel', onUp, { passive: false });
      }, { passive: false });
    }

    // Apply the current stored widths before any interaction starts.
    document.documentElement.style.setProperty(CONSTS.PROP.ROOT_PANE_WIDTH, `${vars.rootPaneWidth}px`);
    document.documentElement.style.setProperty(CONSTS.PROP.LEAF_PANE_WIDTH, `${vars.leafPaneWidth}px`);

    // Wire both pane resizers.
    wirePaneResizer(true);
    wirePaneResizer(false);

    // Re-check layout whenever the window size changes.
    window.addEventListener('resize', checkDualPaneLayout);

    // Correct the layout once immediately on setup.
    checkDualPaneLayout();
  }

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // #endregion 🟥 DUAL PANE RESIZERS

  // #region 🟩 OBSERVERS
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  // Set up layout observers and a mutation observer for the search results window.
  async function setupObservers() {

    // DOM elements that we need in this function
    const reqEls = [
      'TOP',
      'TITLE_AREA',
      'SIDE_NAV',
      'NAV_PATH',
      'DOC_CONTENT',
      'CONTAINER',
      'SEARCH_RESULTS_WIN'
    ];

    // Return the required element keys whose cached elements are missing or disconnected.
    // This avoids awaiting waitFor() for elements that are already available.
    function getMissingEls() {
      const out = [];
      for (const keyName of reqEls) {
        if (!vars.els[keyName] || !vars.els[keyName].isConnected) {
          out.push(keyName);
        }
      }
      return out;
    }

    // Wait only for the required elements that are still missing.
    async function waitMissingEls(keyNames) {
      const jobs = [];
      for (const keyName of keyNames) {
        const selector = CONSTS.SEL[keyName];
        if (!selector) {
          vars.els[keyName] = null;
          continue;
        }
        jobs.push(
          waitFor(selector).then(el => {
            vars.els[keyName] = el || null;
          })
        );
      }
      await Promise.all(jobs);
    }

    // Acquire any missing required elements, waiting for them if needed.
    const missingEls = getMissingEls();
    if (missingEls.length > 0) await waitMissingEls(missingEls);

    // Clicking the title area (top left) navigates to the main page.
    if (vars.els.TITLE_AREA) {
      vars.els.TITLE_AREA.addEventListener('click', evt => {
        // Only left-clicks with no modifier keys
        if (evt.button !== 0 || evt.metaKey || evt.ctrlKey || evt.shiftKey || evt.altKey) return;
        const target = evt.target instanceof Element ? evt.target : null;
        if (target && target.closest('a')) return; // If the user actually clicked an <a> inside, let that link work normally
        window.location.href = CONSTS.DOC_ROOT + 'index.html'; // keeps history
      });
    }

    // Clone the default header and insert it before the container so it can be styled separately.
    // The original header is hidden in CSS.
    let headerClone = null;
    if (vars.els.DOC_CONTENT) {

      // The header is either a direct child of direct grandchild (in case the page is for .md file)
      const defHeader = vars.els.DOC_CONTENT.querySelector(':scope > .header, :scope > * > .header');
      if (defHeader && vars.els.CONTAINER && vars.els.CONTAINER.parentElement) {

        /* Below code removes the redundant words from the header but does not work reliably */
        /*
        const titleEl = defHeader.querySelector('.headertitle > .title');
        if (titleEl) {
          const re = /\s+(?:(?:Class|Struct|Union|Interface|Enum(?:eration)?|Namespace|Module|Concept|Package|Group|Directory|File|Page|Example|Guide|Manual|Service|Record|Category|Protocol|Type|Alias|Typedef|Define|Macro|Variable|Function|Member|Property|Signal|Slot)(?:\s+Template)?)\s+(?:Reference|Documentation)\s*$/i;
          const original = titleEl.textContent || '';
          const cleaned = original.replace(re, '').replace(/\s{2,}/g, ' ').trim();
          if (cleaned !== original) titleEl.textContent = cleaned;
          titleEl.setAttribute('title', cleaned || original);
        }
        */

        headerClone = defHeader.cloneNode(true);
        headerClone.removeAttribute('id');
        vars.els.CONTAINER.parentElement.insertBefore(headerClone, vars.els.CONTAINER);
      }
    }

    // Update the class list to inform CSS that the header clone is available.
    if (headerClone) document.body.classList.add(CONSTS.CLS.DOC_HEADER_AVAILABLE);

    // Observe size changes of the main layout elements.
    const ro = new ResizeObserver(entries => {
      for (const { target, contentRect } of entries) {
        if (target === vars.els.TOP) {
          const hTop = contentRect.height;
          const addCls = (
            vars.els.TITLE_AREA &&
            vars.els.TITLE_AREA.isConnected &&
            Math.abs(vars.els.TITLE_AREA.getBoundingClientRect().height - hTop) > 0.5
          );
          if (addCls) document.body.classList.add(CONSTS.CLS.MAIN_NAV_BELOW_TITLE);
          else document.body.classList.remove(CONSTS.CLS.MAIN_NAV_BELOW_TITLE);
          document.documentElement.style.setProperty(CONSTS.PROP.HEADER_HEIGHT, `${hTop}px`);
        } else if (target === vars.els.SIDE_NAV) {
          document.documentElement.style.setProperty(CONSTS.PROP.SIDE_NAV_WIDTH, `${contentRect.width}px`);
        } else if (target === vars.els.NAV_PATH) {
          document.documentElement.style.setProperty(CONSTS.PROP.FOOTER_HEIGHT, `${contentRect.height}px`);
        } else if (target === headerClone) {
          document.documentElement.style.setProperty(CONSTS.PROP.DOC_HEADER_HEIGHT, `${contentRect.height}px`);
        }
      }
    });

    // Start observing the elements that are available.
    if (vars.els.TOP) ro.observe(vars.els.TOP);
    if (vars.els.SIDE_NAV) ro.observe(vars.els.SIDE_NAV);
    if (vars.els.NAV_PATH) ro.observe(vars.els.NAV_PATH);
    if (headerClone) ro.observe(headerClone);

    // PAGE_NAV is optional, so do not await it.
    // Update body classes so CSS knows whether PAGE_NAV exists and whether it has content.
    // PAGE_NAV can exist before its list items are inserted, so if it is initially empty,
    // observe it briefly and remove the empty-state class once any li appears.
    if (vars.els.PAGE_NAV && vars.els.PAGE_NAV.isConnected && !vars.els.PAGE_NAV.querySelector('li')) {
      document.body.classList.add(CONSTS.CLS.EMPTY_PAGE_NAV);
      let pageNavMoTimeoutId = null;
      const pageNavMo = new MutationObserver(() => {
        if (!vars.els.PAGE_NAV || !vars.els.PAGE_NAV.isConnected) {
          clearTimeout(pageNavMoTimeoutId);
          pageNavMo.disconnect();
          return;
        }

        if (vars.els.PAGE_NAV.querySelector('li')) {
          document.body.classList.remove(CONSTS.CLS.EMPTY_PAGE_NAV);
          clearTimeout(pageNavMoTimeoutId);
          pageNavMo.disconnect();
        }
      });
      pageNavMo.observe(vars.els.PAGE_NAV, { childList: true, subtree: true });
      pageNavMoTimeoutId = setTimeout(() => { pageNavMo.disconnect(); }, 3000);
    }

    // We already waited for SEARCH_RESULTS_WIN. If it is still unavailable, return.
    if (!vars.els.SEARCH_RESULTS_WIN) {
      console.warn('setupObservers(): Search Results Window unavailable.');
      return;
    }

    // Sync the search results window left position and width with the active search box.
    async function syncSearchResultsWindow() {
      // Align the search results window's left position and width to the active search box.

      // Choose which search box container is active:
      // 'searchBoxPos1' for mobile (<768px) and 'searchBoxPos2' for desktop (>=768px).
      const parentId = window.innerWidth < CONSTS.VAL.DESKTOP_VIEW_MIN_WIDTH ? 'searchBoxPos1' : 'searchBoxPos2';

      // Get the search box element inside that container.
      const sBox = await waitFor(`#${parentId} #MSearchBox`);
      if (!sBox) return;

      // Measure the search box's left offset and width.
      const { left, width } = sBox.getBoundingClientRect();

      // Apply the measured left and width to the persistent search results window.
      vars.els.SEARCH_RESULTS_WIN.style.setProperty('left', `${left}px`, 'important');
      vars.els.SEARCH_RESULTS_WIN.style.setProperty('width', `${width}px`, 'important');
      //console.log(`setupObservers() - syncSearchResultsWindow(): Set size X=${left}px & W=${width}px`);
    }

    // Track whether the results window is currently displayed,
    // so we only sync once when it opens.
    let isDisplayed = getComputedStyle(vars.els.SEARCH_RESULTS_WIN).display === 'block';
    if (isDisplayed) {
      void syncSearchResultsWindow().catch((err) => {
        console.error('setupObservers(): syncSearchResultsWindow() failed', err);
      });
    }

    // Observe style changes on the search results window to detect show/hide.
    const mo = new MutationObserver(records => {
      // Only react when the window transitions to display:block.
      for (const rec of records) {
        // Only handle style attribute mutations.
        if (rec.attributeName !== 'style') continue;
        const nowDisplay = getComputedStyle(vars.els.SEARCH_RESULTS_WIN).display === 'block';
        if (!isDisplayed && nowDisplay) {
          // Window just shown: sync size.
          isDisplayed = true;
          void syncSearchResultsWindow().catch((err) => {
            console.error('setupObservers(): syncSearchResultsWindow() failed', err);
          });
        } else if (isDisplayed && !nowDisplay) {
          // Window just hidden: update state.
          isDisplayed = false;
        }
        // otherwise ignore
      }
    });

    // Begin observing style mutations on the search results window.
    mo.observe(vars.els.SEARCH_RESULTS_WIN, { attributes: true, attributeFilter: ['style'] });
  }

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // #endregion 🟥 OBSERVERS

  // #region 🟩 SEARCHBAR TWEAK
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  // Replace the magnifier search icon with relevant text.
  async function searchbarTweak() {

    // Replace the search category icon with a text label that reflects the current search context.
    // By default, Doxygen shows only a magnifying-glass icon here.

    // Wait until test() returns true.
    async function waitUntil(test) {
      const start = performance.now();
      while (!test()) {
        const elapsed = performance.now() - start;
        if (elapsed > CONSTS.VAL.TIMEOUT_MS) return false;
        await new Promise(requestAnimationFrame);
      }
      return true;
    }

    // Readiness condition for update().
    const updateCondition = () => !!(
      window.searchBox &&
      typeof window.searchBox.searchIndex === 'number' &&
      window.indexSectionLabels
    );

    // Sync the '--dp-search-field' CSS variable based on the current search category.
    async function update() {
      const okToUpdate = updateCondition() || await waitUntil(updateCondition);
      if (!okToUpdate) {
        console.error('searchbarTweak() - update(): Timed out waiting for conditions');
        return;
      }
      const label = window.indexSectionLabels[window.searchBox.searchIndex] || 'All';
      document.documentElement.style.setProperty(CONSTS.PROP.SEARCH_FIELD, `"${label}:"`);
    }

    // Readiness condition for attaching to OnSelectItem.
    const attachCondition = () => !!(
      window.searchBox &&
      typeof window.searchBox.OnSelectItem === 'function'
    );

    // Ensure window.searchBox.OnSelectItem exists before hooking into it.
    const okToAttach = await waitUntil(attachCondition);
    if (!okToAttach) {
      console.error('searchbarTweak(): Timed out waiting for searchBox.OnSelectItem.');
      return;
    }

    // Override OnSelectItem to call update() whenever the user selects 
    // a different category from the dropdown.
    const orig = window.searchBox.OnSelectItem;
    window.searchBox.OnSelectItem = function (id) {
      const ret = orig.call(this, id);
      //console.log('searchbarTweak(): Update On Select Item Triggered');
      void update().catch((err) => {
        console.error('searchbarTweak(): update() failed', err);
      });
      return ret;
    };
    //console.log('searchbarTweak(): SUCCESS');

    // Run one initial sync.
    await update();
  }

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // #endregion 🟥 SEARCHBAR TWEAK

  // #region 🟩 INIT OPTIONS
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  // Initialize options button
  async function initOptions() {

    if (document.getElementById('dp-opt-backdrop')) return;

    var backdrop = document.createElement('div');
    backdrop.id = 'dp-opt-backdrop';

    var modal = document.createElement('div');
    modal.id = 'dp-opt-modal';
    modal.setAttribute('role', 'dialog');
    modal.setAttribute('aria-modal', 'true');

    var form = document.createElement('div');
    form.id = 'dp-opt-form';

    // Doxy-Plus Primary Nav checkbox
    var rowRoot = document.createElement('div');
    rowRoot.className = 'dp-opt-row';
    var lblRoot = document.createElement('label');
    lblRoot.className = 'dp-opt-label';
    var cbRoot = document.createElement('input');
    cbRoot.type = 'checkbox';
    cbRoot.id = 'dp-opt-cb-root';
    cbRoot.checked = vars.dualNav;
    lblRoot.appendChild(cbRoot);
    lblRoot.appendChild(document.createTextNode('Doxy-Plus Primary Nav'));
    rowRoot.appendChild(lblRoot);

    // Doxy-Plus Members List checkbox
    var rowLeaf = document.createElement('div');
    rowLeaf.className = 'dp-opt-row';
    var lblLeaf = document.createElement('label');
    lblLeaf.className = 'dp-opt-label';
    var cbLeaf = document.createElement('input');
    cbLeaf.type = 'checkbox';
    cbLeaf.id = 'dp-opt-cb-leaf';
    cbLeaf.checked = vars.showLeafPane;
    lblLeaf.appendChild(cbLeaf);
    lblLeaf.appendChild(document.createTextNode('Doxy-Plus Members List'));
    rowLeaf.appendChild(lblLeaf);

    // Page Nav checkbox
    var rowPage = document.createElement('div');
    rowPage.className = 'dp-opt-row';
    var lblPage = document.createElement('label');
    lblPage.className = 'dp-opt-label';
    var cbPage = document.createElement('input');
    cbPage.type = 'checkbox';
    cbPage.id = 'dp-opt-cb-page';
    cbPage.checked = vars.showPageNav;
    lblPage.appendChild(cbPage);
    lblPage.appendChild(document.createTextNode('Page Table of Contents'));
    rowPage.appendChild(lblPage);

    // Auto Hide checkbox
    var rowAuto = document.createElement('div');
    rowAuto.className = 'dp-opt-row';
    var lblAuto = document.createElement('label');
    lblAuto.className = 'dp-opt-label';
    var cbAuto = document.createElement('input');
    cbAuto.type = 'checkbox';
    cbAuto.id = 'dp-opt-cb-auto';
    cbAuto.checked = vars.autoHideLists;
    lblAuto.appendChild(cbAuto);
    lblAuto.appendChild(document.createTextNode('Auto Hide On-Page Lists'));
    rowAuto.appendChild(lblAuto);

    function setDoxyPlusCss() {
      if (vars.dualNav) {
        document.body.classList.add(CONSTS.CLS.DUAL_NAV_ACTIVE);
        cbLeaf.disabled = false;
        lblLeaf.classList.remove('dp-opt-disabled');
        if (vars.showLeafPane) document.body.classList.add(CONSTS.CLS.SHOW_LEAF_PANE);
        else document.body.classList.remove(CONSTS.CLS.SHOW_LEAF_PANE);
      }
      else {
        document.body.classList.remove(CONSTS.CLS.DUAL_NAV_ACTIVE);
        document.body.classList.remove(CONSTS.CLS.SHOW_LEAF_PANE);
        cbLeaf.disabled = true;
        lblLeaf.classList.add('dp-opt-disabled');
      }
    }
    setDoxyPlusCss();

    function setPageNavCss() {
      if (vars.showPageNav) document.body.classList.add(CONSTS.CLS.SHOW_PAGE_NAV);
      else document.body.classList.remove(CONSTS.CLS.SHOW_PAGE_NAV);
    }
    setPageNavCss();

    function setAutoHideCss() {
      if (vars.autoHideLists) document.body.classList.add(CONSTS.CLS.AUTO_HIDE_LISTS);
      else document.body.classList.remove(CONSTS.CLS.AUTO_HIDE_LISTS);
    }
    setAutoHideCss();

    function onCbRootToggle() {
      vars.dualNav = cbRoot.checked;
      storage.dualNav.save(vars.dualNav);
      setDoxyPlusCss();
    }
    cbRoot.addEventListener('change', onCbRootToggle);

    function onCbLeafToggle() {
      vars.showLeafPane = cbLeaf.checked;
      storage.showLeafPane.save(vars.showLeafPane);
      setDoxyPlusCss();
    }
    cbLeaf.addEventListener('change', onCbLeafToggle);

    function onCbPageToggle() {
      vars.showPageNav = cbPage.checked;
      storage.showPageNav.save(vars.showPageNav);
      setPageNavCss();
    }
    cbPage.addEventListener('change', onCbPageToggle);

    function onCbAutoToggle() {
      vars.autoHideLists = cbAuto.checked;
      storage.autoHideLists.save(vars.autoHideLists);
      setAutoHideCss();
    }
    cbAuto.addEventListener('change', onCbAutoToggle);

    // Assemble
    form.appendChild(rowRoot);
    form.appendChild(rowLeaf);
    form.appendChild(rowPage);
    form.appendChild(rowAuto);
    modal.appendChild(form);
    backdrop.appendChild(modal);
    document.body.appendChild(backdrop);

    // Modal open/close & positioning
    var lastFocus = null;

    function openModal() {
      var b = document.getElementById('dp-opt-backdrop');
      if (!b) return;

      document.body.classList.add('dp-opt-backdrop-blur');
      b.setAttribute('data-dp-opt-win-open', '');

      // default: centered
      modal.classList.remove('dp-opt-win-at-anchor');
      modal.style.left = '';
      modal.style.top = '';

      // Align right edge to #dp-options-btn if present
      var anchor = document.getElementById('dp-options-btn');
      if (anchor) {
        modal.classList.add('dp-opt-win-at-anchor');
        modal.style.left = '0px';
        modal.style.top = '0px';

        var btn = anchor.getBoundingClientRect();
        var mw = modal.offsetWidth;
        var mh = modal.offsetHeight;

        var desiredLeft = btn.right - mw;  /* align right edges */
        var desiredTop = btn.bottom + 8;   /* drop below a bit */

        var minLeft = 8;
        var maxLeft = Math.max(8, window.innerWidth - mw - 8);
        var minTop = 8;
        var maxTop = Math.max(8, window.innerHeight - mh - 8);

        var finalLeft = Math.min(Math.max(desiredLeft, minLeft), maxLeft);
        var finalTop = Math.min(Math.max(desiredTop, minTop), maxTop);

        modal.style.left = finalLeft + 'px';
        modal.style.top = finalTop + 'px';
      }

      // When the page is opened htrough history the checkboxes are not
      // correctly checked when shown, so we set the correct checkbox
      // state each time the checkboxes are displayed.
      cbRoot.checked = vars.dualNav;
      cbLeaf.checked = vars.showLeafPane;
      cbPage.checked = vars.showPageNav;
      cbAuto.checked = vars.autoHideLists;

      lastFocus = document.activeElement;
      modal.setAttribute('tabindex', '-1');
      modal.focus();

      document.addEventListener('keydown', onKey);
      b.addEventListener('click', onBackdropClick);
    }

    function closeModal() {
      var b = document.getElementById('dp-opt-backdrop');
      if (!b) return;
      document.removeEventListener('keydown', onKey);
      b.removeEventListener('click', onBackdropClick);
      b.removeAttribute('data-dp-opt-win-open');
      document.body.classList.remove('dp-opt-backdrop-blur');
      if (lastFocus && typeof lastFocus.focus === 'function') lastFocus.focus();
    }

    function onBackdropClick(e) {
      if (e.target === backdrop) closeModal(); /* click-outside closes */
    }

    function onKey(e) {
      if (e.key !== 'Escape') return;
      e.preventDefault();
      closeModal();
    }

    const OPTIONS_SVG = (() => {

      const NS = "http://www.w3.org/2000/svg";

      function svgCreateLine(x1, y1, x2, y2, width) {
        const l = document.createElementNS(NS, "line");
        l.setAttribute("x1", x1);
        l.setAttribute("y1", y1);
        l.setAttribute("x2", x2);
        l.setAttribute("y2", y2);
        l.setAttribute("stroke-linecap", "round");
        l.setAttribute("stroke", "currentColor");
        l.setAttribute("stroke-width", width);
        l.setAttribute("vector-effect", "non-scaling-stroke");
        l.setAttribute("shape-rendering", "geometricPrecision");
        return l;
      }

      function svgCreateCircle(x, y, r) {
        const c = document.createElementNS(NS, "circle");
        c.setAttribute("cx", x);
        c.setAttribute("cy", y);
        c.setAttribute("r", r);
        c.setAttribute("fill", "currentColor");
        return c;
      }

      const y1 = '4';
      const y2 = '12';
      const y3 = '20';
      const w = '2';
      const r = '2.5';

      var svg = document.createElementNS(NS, "svg");
      svg.setAttribute("viewBox", "0 0 24 24");
      svg.setAttribute("aria-hidden", "true");
      svg.setAttribute("focusable", "false");

      svg.appendChild(svgCreateLine('2', y1, '3', y1, w));
      svg.appendChild(svgCreateLine('13', y1, '22', y1, w));
      svg.appendChild(svgCreateLine('2', y2, '11', y2, w));
      svg.appendChild(svgCreateLine('21', y2, '22', y2, w));
      svg.appendChild(svgCreateLine('2', y3, '3', y3, w));
      svg.appendChild(svgCreateLine('13', y3, '22', y3, w));

      svg.appendChild(svgCreateCircle('8', y1, r));
      svg.appendChild(svgCreateCircle('16', y2, r));
      svg.appendChild(svgCreateCircle('8', y3, r));

      return svg;
    })();

    // Make the options button
    const mo = new MutationObserver(() => {
      void setup().catch((err) => {
        console.error('initOptions(): setup() failed', err);
      });
    });

    async function setup() {

      // Wait for the desktop search box container
      const itemSearchBox = await waitFor('#searchBoxPos2');
      if (!itemSearchBox) {
        console.error(`initOptions() - setup(): '#searchBoxPos2' not found`);
        return;
      }

      mo.disconnect(); // Prevent handling our own DOM changes

      // Only show toggle on desktop widths (>=768px)
      const winWidth = window.innerWidth || document.documentElement.clientWidth;
      if (winWidth >= CONSTS.VAL.DESKTOP_VIEW_MIN_WIDTH) {

        // Try to find an existing toggle button
        let btn = itemSearchBox.querySelector('#dp-options-btn');
        if (btn) {
          // Move existing button to end of the search box and update its icon
          itemSearchBox.appendChild(btn);
          //console.log('initOptions() - setup(): Reposition');
        }
        else {
          // Create a new toggle button and icon
          btn = document.createElement("button");
          btn.id = 'dp-options-btn';
          btn.type = "button";
          btn.setAttribute("aria-label", "Settings");
          btn.title = 'Show Options Window';
          btn.appendChild(OPTIONS_SVG);
          btn.addEventListener("click", openModal);
          itemSearchBox.appendChild(btn);
          //console.log('initOptions() - setup(): New Button');
        }
      }
      //else {
      //  console.log('initOptions() - setup(): Width < 768');
      //}

      // Observe the search box for childList changes (Doxygen rebuilds on resize)
      mo.observe(itemSearchBox, { childList: true });
    }
    void setup().catch((err) => {
      console.error('initOptions(): setup() failed', err);
    });
    //console.log('initOptions(): SUCCESS');
  }

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // #endregion 🟥 INIT OPTIONS

  // #region 🟩 DATA DISPLAY
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  // Displays vars.htmlData in console
  function dispHtmlData() {
    console.group('HTML Data Array (vars.htmlData in code)');
    console.log(`- Modified version of the Doxygen NAVTREE`,
      `\n- Excludes:`,
      `\n  - Any href values containing hash links, such as "abc.html#def"`,
      `\n  - Duplicate nodes with the same href`,
      `\n- Node format:`,
      `\n  - node[0]: Name (non-empty string)`,
      `\n  - node[1]: Prefix (null or non-empty string)`,
      `\n  - node[2]: Href (string ending in .html) or Child Nodes (non-empty array)`);
    debugTreeData(vars.htmlData, 'html');
    console.groupEnd();
  }

  // Displays vars.hashData in console
  function dispHashData() {
    console.group('Hash Data Array (vars.hashData in code)');
    console.log(`- Generated hash-data array for the current page`,
      `\n- Contains:`,
      `\n  - Hash values, such as "#abcdefg", as href values`,
      `\n  - No duplicate nodes with the same href`,
      `\n- Node format:`,
      `\n  - node[0]: Name (non-empty string)`,
      `\n  - node[1]: Prefix (null or non-empty string)`,
      `\n  - node[2]: Hash (string starting with #) or Child Nodes (non-empty array)`);
    debugTreeData(vars.hashData, 'hash');
    console.groupEnd();
  }

  // Displays default Doxygen NAVTREE in console
  async function dispDoxygenNavtree() {
    const doxygenNavtree = await buildDoxygenNavtree(false);
    console.group('Default Doxygen NAVTREE');
    console.log(`- Fully loaded Default Doxygen NAVTREE`,
      `\n- Node format:`,
      `\n  - node[0]: Name (string)`,
      `\n  - node[1]: Href (string)`,
      `\n  - node[2]: Child Nodes (array)`,
      `\nNOTE: Due to a bug in Doxygen, node[2] can sometimes contain a string, but this is harmless.`);
    debugTreeData(doxygenNavtree, 'default');
    console.groupEnd();
  }

  // Displays pruned Doxygen NAVTREE in console
  async function dispPrunedDoxygenNavtree() {
    const prunedDoxygenNavtree = await buildDoxygenNavtree(true);
    console.group('Pruned Doxygen NAVTREE');
    console.log(`- Fully loaded Pruned Doxygen NAVTREE`,
      `\n- Pruned:`,
      `\n  - All malformed nodes are removed`,
      `\n  - Nodes with hash hrefs (for example, "abc.html#def") are removed`,
      `\n  - Nodes with no child and no href are removed`,
      `\n- Node format:`,
      `\n  - node[0]: Name (string)`,
      `\n  - node[1]: Href (string)`,
      `\n  - node[2]: Child Nodes (array)`,
      `\nNOTE: Due to a bug in Doxygen, node[2] can sometimes contain a string, but this is harmless.`);
    debugTreeData(prunedDoxygenNavtree, 'default');
    console.groupEnd();
  }

  // Displays html not found list in console
  function dispHtmlNotFoundList() {
    const list = storage.htmlNotFound.load();
    let idx = 0;
    console.group('HTML Not Found List');
    console.log(`- List of all html pages that were not found in Doxygen NAVTREE`,
      `\n- It is generated when generating modified html data from Doxygen NAVTREE`,
      `\n- Doxygen generates some html pages on demand, and so not all pages exist everytime`,
      `\n  - Examples: 'hierarchy.html', 'todo.html', 'test.html', etc.`,
      `\n- Having a non-empty not found list does not mean there was an error in generating html data`,
      `\n- Format: '#. Name Path: href'`);
    if (Array.isArray(list) && list.length > 0) {
      for (const node of list) {
        console.log('%s%c%s', `${++idx}. ${node[0]}: `, 'color: LinkText;', String(node[1]));
      }
    }
    if (idx === 0) console.log('--- HTML Not Found List is Empty ---');
    console.groupEnd();
  }

  // Displays html ignored list in console
  function dispHtmlIgnoredList() {
    const list = storage.htmlIgnored.load();
    let idx = 0;
    console.group('HTML Ignored List');
    console.log(`- List of all html pages that were deliberately ignored`,
      `\n- It is generated when generating modified html data from Doxygen NAVTREE`,
      `\n- This list mostly has directory pages, most of the information can be viewed in Files List`,
      `\n  - All href strings with 'dir_' anywhere in it means a directory page`,
      `\n- These html pages provide no additional information and should be ignored`,
      `\n- Format: '#. Name Path: href'`);
    if (Array.isArray(list) && list.length > 0) {
      for (const node of list) {
        console.log('%s%c%s', `${++idx}. ${node[0]}: `, 'color: LinkText;', String(node[1]));
      }
    }
    if (idx === 0) console.log('--- HTML Ignored List is Empty ---');
    console.groupEnd();
  }

  // Displays html list of pages that are not in vars.htmlData and not in the HTML ignored list
  async function dispHtmlMissedList() {

    const have = new Set();

    (function collectNormalHrefs(branch) {
      if (!Array.isArray(branch) || branch.length === 0) return;
      for (const node of branch) {
        if (Array.isArray(node[2])) collectNormalHrefs(node[2]);
        else if (typeof node[2] === 'string' && !have.has(node[2])) have.add(node[2]);
      }
    })(vars.htmlData);

    const ignoredList = storage.htmlIgnored.load();
    if (Array.isArray(ignoredList) && ignoredList.length > 0) {
      for (const node of ignoredList) {
        if (!have.has(node[1])) have.add(node[1]);
      }
    }

    const prunedDoxygenNavtree = await buildDoxygenNavtree(true);

    let idx = 0;
    console.group('HTML Missed List');
    console.log(`- List of all html pages that are present in pruned Doxygen NAVTREE but are not included in:`,
      `\n  - Generated html data array (i.e. vars.htmlData), or`,
      `\n  - In html Ignored List`,
      `\n- This list contains directory pages (href string with 'dir_' in it) from Markdown only branches`,
      `\n  - The directory pages of Markdown only branches are designed to be without text contents and should be ignored`,
      `\n- Format: '#. Name Path: href'`);

    (function collectMissedHrefs(branch, prefix = '') {
      if (!Array.isArray(branch) || branch.length === 0) return;
      for (const node of branch) {
        const name = prefix ? `${prefix} > ${node[0]}` : node[0];
        if (typeof node[1] === 'string' && node[1].length > 0 && !have.has(node[1])) {
          console.log('%s%c%s', `${++idx}. ${name}: `, 'color: LinkText;', String(node[1]));
        }
        if (Array.isArray(node[2])) collectMissedHrefs(node[2], name);
      }
    })(prunedDoxygenNavtree);
    if (idx === 0) console.log('--- HTML Missed List is Empty ---');
    console.groupEnd();
  }

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // #endregion 🟥 DATA TABLES

  // #region 🟩 CONSOLE OBJECT
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  // Create the console debug object as "dp" or "debugDoxyPlus".
  function createConsoleObject() {

    // Create the build sub-object
    function makeBuildObj() {
      const buildObj = Object.create(null);

      Object.defineProperty(buildObj, 'about', {
        get() {
          console.group('About Build');
          console.log('This section displays the current data in the page:',
            '\n- doxygen_navtree        : Non-altered Doxygen NAVTREE',
            '\n- doxygen_navtree_pruned : Altered Doxygen NAVTREE with hash links and empty nodes removed',
            '\n- hash_data              : Hash links for the current page',
            '\n- html_data              : Full html pages list for this project, generated by modifying pruned Doxygen NAVTREE',
            '\n- html_ignored           : The html pages that were deliberately ignored while generating full html pages list',
            '\n- html_missed            : A list of all html pages that are in pruned Doxygen NAVTREE but not in generated html pages or ignored list',
            '\n- html_not_found         : A list of all html pages that were not found in pruned Doxygen NAVTREE while generating full html pages list');
          console.groupEnd();
        },
        configurable: true,
        enumerable: true
      });

      Object.defineProperty(buildObj, 'html_data', {
        get() { dispHtmlData(); },
        configurable: true,
        enumerable: true
      });

      Object.defineProperty(buildObj, 'hash_data', {
        get() { dispHashData(); },
        configurable: true,
        enumerable: true
      });

      Object.defineProperty(buildObj, 'doxygen_navtree', {
        get() { setTimeout(() => { void dispDoxygenNavtree(); }, 0); },
        configurable: true,
        enumerable: true
      });

      Object.defineProperty(buildObj, 'doxygen_navtree_pruned', {
        get() { setTimeout(() => { void dispPrunedDoxygenNavtree(); }, 0); },
        configurable: true,
        enumerable: true
      });

      Object.defineProperty(buildObj, 'html_not_found', {
        get() { dispHtmlNotFoundList(); },
        configurable: true,
        enumerable: true
      });

      Object.defineProperty(buildObj, 'html_ignored', {
        get() { dispHtmlIgnoredList(); },
        configurable: true,
        enumerable: true
      });

      Object.defineProperty(buildObj, 'html_missed', {
        get() { setTimeout(() => { void dispHtmlMissedList(); }, 0); },
        configurable: true,
        enumerable: true
      });

      return buildObj;
    }

    // Create store sub-object
    function makeStoreObj() {

      // Create clear sub-object
      function makeClearObj() {
        const clearObj = Object.create(null);

        Object.defineProperty(clearObj, 'about', {
          get() {
            console.group('About Clear');
            console.log('This section clears the stored keys',
              '\n- all             : Clears all storage i.e. browser localStorage and sessionStorage, all key-value pairs on this origin and for this tab is removed',
              '\n- local_storage   : Clears browser localStorage but not sessionStorage, all key-value pairs on this origin is removed',
              '\n- session_storage : Clears browser sessionStorage, all key-value pairs for this tab is removed',
              '\n- origin_storage  : Clears all key-value pairs on this origin (only for keys stored using store.js)',
              '\n- project_storage : Clears all key-value pairs for this project only (only for keys stored using store.js), does not affect other projects on this origin');
            console.log('%cNOTE: When any clear option is used, a confirmation message box is shown on the main browser page. The selected command is executed only after the user clicks "OK" in that message box.', 'color: orange; font-weight: bold;');
            console.groupEnd();
          },
          configurable: true,
          enumerable: true
        });

        Object.defineProperty(clearObj, 'all', {
          get() { return storage.internals.clearAllKeys(); },
          configurable: true,
          enumerable: true
        });

        Object.defineProperty(clearObj, 'local_storage', {
          get() { return storage.internals.clearLocalStorage(); },
          configurable: true,
          enumerable: true
        });

        Object.defineProperty(clearObj, 'session_storage', {
          get() { return storage.internals.clearSessionStorage(); },
          configurable: true,
          enumerable: true
        });

        Object.defineProperty(clearObj, 'origin_storage', {
          get() { return storage.internals.clearOriginStorage(); },
          configurable: true,
          enumerable: true
        });

        Object.defineProperty(clearObj, 'project_storage', {
          get() { return storage.internals.clearProjectStorage(); },
          configurable: true,
          enumerable: true
        });

        return clearObj;
      }

      const storeObj = Object.create(null);
      Object.defineProperty(storeObj, 'clear', { value: makeClearObj(), configurable: true, enumerable: true });

      Object.defineProperty(storeObj, 'about', {
        get() {
          console.group('About Store');
          console.log('This section displays the stored key-value pairs',
            '\n- clear                        : Clears stored key-value pairs (see clear.about)',
            '\n- origin_storage               : All key-value pairs stored on this origin using store.js',
            '\n- project_storage              : All key-value pairs stored for this project using store.js',
            '\n- local_storage                : All key-value pairs stored in browser localStorage on this origin',
            '\n- local_storage_non_store_js   : All key-value pairs stored (without using store.js) in browser localStorage on this origin',
            '\n- session_storage              : All key-value pairs stored in browser sessionStorage',
            '\n- session_storage_non_store_js : All key-value pairs stored (without using store.js) in browser sessionStorage');
          console.groupEnd();
        },
        configurable: true,
        enumerable: true
      });

      Object.defineProperty(storeObj, 'origin_storage', {
        get() { return storage.internals.dispOriginStorage(); },
        configurable: true,
        enumerable: true
      });

      Object.defineProperty(storeObj, 'project_storage', {
        get() { return storage.internals.dispProjectStorage(); },
        configurable: true,
        enumerable: true
      });

      Object.defineProperty(storeObj, 'local_storage', {
        get() { return storage.internals.dispLocalStorage(); },
        configurable: true,
        enumerable: true
      });

      Object.defineProperty(storeObj, 'local_storage_non_store_js', {
        get() { return storage.internals.dispLocalStorageNonStoreJS(); },
        configurable: true,
        enumerable: true
      });

      Object.defineProperty(storeObj, 'session_storage', {
        get() { return storage.internals.dispSessionStorage(); },
        configurable: true,
        enumerable: true
      });

      Object.defineProperty(storeObj, 'session_storage_non_store_js', {
        get() { return storage.internals.dispSessionStorageNonStoreJS(); },
        configurable: true,
        enumerable: true
      });

      return storeObj;
    }

    // Main object
    const mainObj = Object.create(null);

    // About project
    Object.defineProperty(mainObj, 'about', {
      get() {
        const gap = '\n' + ' '.repeat(19); // Details line start position
        console.group('About Project');
        console.log('%s%c%s%c%s%c%s', 'Date-Time        : ', 'color: LinkText;', storage.internals.dateTime, '', gap,
          'color: GrayText; font-size: 90%;', 'Doxygen generation date-time.');
        console.log('%s%c%s%c%s%c%s', 'Doc Root         : ', 'color: LinkText;', CONSTS.DOC_ROOT, '', gap,
          'color: GrayText; font-size: 90%;', 'Origin URL plus the repository name (hosted online) or local folder path (hosted offline).');
        console.log('%s%c%s%c%s%c%s', 'Storage Namespace: ', 'color: LinkText;', storage.internals.namespace, '', gap,
          'color: GrayText; font-size: 90%;', 'Namespace used to store project data through the store.js namespace feature.');

        console.groupEnd();
      },
      configurable: true,
      enumerable: true
    });

    Object.defineProperty(mainObj, 'build', { value: makeBuildObj(), configurable: true, enumerable: true });
    Object.defineProperty(mainObj, 'store', { value: makeStoreObj(), configurable: true, enumerable: true });

    // Assign "dp" as the console debug handler if it is not already defined.
    // If "dp" is already defined, use "debugDoxyPlus" instead.
    if (window.dp !== undefined) {
      console.log('● Full Reload: "dp" already defined - "debugDoxyPlus" is the console debug handler');
      window.debugDoxyPlus = mainObj;
      vars.consoleObjectName = 'debugDoxyPlus';
    }
    else {
      console.log('● Full Reload: "dp" is the console debug handler');
      window.dp = mainObj;
      vars.consoleObjectName = 'dp';
    }
  }

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // #endregion 🟥 CONSOLE OBJECT

  // #region 🟩 DOXSECTION CUSTOMIZATION
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  // Customizes doxsection on Markdown pages for better view
  async function doxsectionsCustomizations() {

    // Only needed on pages that have doxsections e.g. Markdown pages
    if (!vars.pageHasDoxsections) return;

    // Return if page content is not found
    if (!vars.els.PAGE_CONTENT || !vars.els.PAGE_CONTENT.isConnected) {
      vars.els.PAGE_CONTENT = await waitFor(CONSTS.SEL.PAGE_CONTENT);
      if (!vars.els.PAGE_CONTENT) {
        console.warn(`doxsectionsCustomizations(): Selector ${CONSTS.SEL.PAGE_CONTENT} not found.`);
        return;
      }
    }

    // Query all doxsection headings
    const doxHeads = vars.els.PAGE_CONTENT.querySelectorAll(':is(h1, h2, h3, h4, h5, h6).doxsection');
    const headStack = [];
    for (let ii = 0; ii < doxHeads.length; ++ii) {
      const heading = doxHeads[ii];
      const level = Number(heading.tagName.slice(1));
      const text = heading.textContent.replace(/\s+/g, ' ').trim();
      if (!text) continue;

      headStack[level - 1] = text;
      headStack.length = level;

      const parentPath = headStack.slice(0, level - 1).join(' > ');

      // For a child doxsection attach its parent path
      // dataset.dpDoxsectionParentPath = 'data-dp-doxsection-parent-path' in html
      if (parentPath) heading.dataset.dpDoxsectionParentPath = parentPath;
      else delete heading.dataset.dpDoxsectionParentPath;

      // Set has children flags for parent sections
      // dataset.dpDoxsectionHasChildren = 'data-dp-doxsection-has-children' in html
      const nextHeading = doxHeads[ii + 1];
      if (nextHeading) {
        const nextLevel = Number(nextHeading.tagName.slice(1));
        if (nextLevel > level) heading.dataset.dpDoxsectionHasChildren = 'true';
        else delete heading.dataset.dpDoxsectionHasChildren;
      } else {
        delete heading.dataset.dpDoxsectionHasChildren;
      }
    }

    // We wrap child doxsections in additional 'div' so that we can change
    // its margin and add a vertical line on the left to show the depth of
    // child sections.
    const doxNodes = Array.from(vars.els.PAGE_CONTENT.children);
    const nodeStack = [];
    for (const doxNode of doxNodes) {
      const isHeading = doxNode.matches(':is(h1, h2, h3, h4, h5, h6).doxsection');
      const level = isHeading ? Number(doxNode.tagName.slice(1)) : 0;

      if (isHeading) {
        while (nodeStack.length && nodeStack[nodeStack.length - 1].level >= level) {
          nodeStack.pop();
        }

        if (nodeStack.length) {
          nodeStack[nodeStack.length - 1].wrap.appendChild(doxNode);
        }

        if (doxNode.dataset.dpDoxsectionHasChildren === 'true') {
          const wrap = document.createElement('div');
          wrap.className = CONSTS.CLS.DOXSECTION_CHILDREN;
          wrap.dataset.dpDoxsectionChildrenOf = doxNode.tagName.toLowerCase();

          if (nodeStack.length) {
            nodeStack[nodeStack.length - 1].wrap.appendChild(wrap);
          } else {
            doxNode.insertAdjacentElement('afterend', wrap);
          }

          nodeStack.push({ level, wrap });
        }

        continue;
      }

      if (nodeStack.length) {
        nodeStack[nodeStack.length - 1].wrap.appendChild(doxNode);
      }
    }
  }

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // #endregion 🟥 DOXSECTION CUSTOMIZATION

  // #region 🟩 VERTICAL SCROLL
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  // Restore a page's scroll position on load and save its scroll position on scroll and page hide.
  function restoreAndObserveVScroll() {

    // vars.els.DOC_CONTENT has already been verified and waited for (if needed)
    // in the setupObservers() function, so no need to do that here, just
    // return if it is not there or if it is not connected.
    if (!vars.els.DOC_CONTENT || !vars.els.DOC_CONTENT.isConnected) return;

    // Timer ID used to debounce scroll-position saves.
    let saveScrollTimer = null;

    // Retry restoring until vars.els.DOC_CONTENT is tall enough or the retry limit is reached.
    function restoreScroll() {
      const savedTop = Number(storage.pageScrollVPos.load());
      if (!Number.isFinite(savedTop) || savedTop < 0) return;

      let tries = 0;

      function tryRestore() {
        vars.els.DOC_CONTENT.scrollTop = savedTop;
        const maxTop = vars.els.DOC_CONTENT.scrollHeight - vars.els.DOC_CONTENT.clientHeight;
        if (maxTop >= savedTop || tries >= 50) return;
        tries += 1;
        requestAnimationFrame(tryRestore);
      }

      tryRestore();
    }

    // Save the current vertical scroll position of vars.els.DOC_CONTENT.
    function saveScroll() {
      const val = vars.els.DOC_CONTENT.scrollTop;
      if (val > 0) storage.pageScrollVPos.save(val);
      else storage.pageScrollVPos.remove();
    }

    // Debounce scroll-triggered saves so storage is written only after scrolling settles.
    function onScroll() {
      window.clearTimeout(saveScrollTimer);
      saveScrollTimer = window.setTimeout(() => {
        saveScrollTimer = null;
        saveScroll();
      }, 200);
    }

    // Clear any pending debounce timer and save the current scroll position.
    function saveScrollNow() {
      window.clearTimeout(saveScrollTimer);
      saveScrollTimer = null;
      saveScroll();
    }

    // Restore initially
    restoreScroll();

    // Add listeners for saving
    vars.els.DOC_CONTENT.addEventListener('scroll', onScroll, { passive: true });
    window.addEventListener('pagehide', saveScrollNow);
  }

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // #endregion 🟥 VERTICAL SCROLL

  // #region 🟩 MARKDOWN PAGENAV
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  // Attches a class to markdown page nav header items, because Doxygen does not do that 
  function markdownPagenav() {

    // NOTE: doxsectionsCustomizations() function should have run before running this function

    // Only run on pages that have doxsection (e.g. Markdown pages) and when page content is available
    if (!vars.pageHasDoxsections || !vars.els.PAGE_CONTENT || !vars.els.PAGE_CONTENT.isConnected) return;

    // Function to attach markdown page nav haeder item correctly
    function attchPagenavItemClass() {

      // The headings in the main page that have children are already
      // customized with `data-dp-doxsection-has-children="true"` in
      // doxygenCustomization() function.
      const headings = vars.els.PAGE_CONTENT.querySelectorAll(
        ':is(h1, h2, h3, h4, h5, h6).doxsection[data-dp-doxsection-has-children="true"]'
      );

      // For each heading get its anchor then find the same anchor in page nav
      // and attach a class called 'dp-markdown-page-nav-header-item' to it.
      // Page nav items with 'dp-markdown-page-nav-header-item' is customized
      // using CSS.
      for (const heading of headings) {
        const anchor = heading.querySelector(':scope > a.anchor[id]');
        if (!anchor) continue;
        const navItem = document.getElementById(`nav-${anchor.id}`);
        if (!navItem || !vars.els.PAGE_NAV.contains(navItem)) continue;
        navItem.classList.add(CONSTS.CLS.MARKDOWN_PAGE_NAV_HEADER_ITEM);
      }
    }

    // If page nav is available and has li items then proceed immediately otherwise use
    // a mutation observer to wait for it. The page nav does not immediately gets populated
    // with li items on page load and so waiting for it is necessary.
    if (vars.els.PAGE_NAV && vars.els.PAGE_NAV.isConnected && vars.els.PAGE_NAV.querySelector('li')) {
      attchPagenavItemClass();
    }
    else {
      let pageNavItemMoTimeoutId = null;
      const pageNavItemMo = new MutationObserver(() => {
        if (!vars.els.PAGE_NAV || !vars.els.PAGE_NAV.isConnected) {
          clearTimeout(pageNavItemMoTimeoutId);
          pageNavItemMo.disconnect();
          return;
        }

        if (vars.els.PAGE_NAV.querySelector('li')) {
          attchPagenavItemClass();
          clearTimeout(pageNavItemMoTimeoutId);
          pageNavItemMo.disconnect();
        }
      });
      pageNavItemMo.observe(vars.els.PAGE_NAV, { childList: true, subtree: true });
      pageNavItemMoTimeoutId = setTimeout(() => { pageNavItemMo.disconnect(); }, 3000);
    }
  }

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // #endregion 🟥 MARKDOWN PAGENAV

  // #region 🟩 DOCUMENT READY CALLS
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  // Run the main initialization sequence once the DOM is ready.
  async function docOnReady() {
    // Build or load the root-pane HTML data first because:
    // 1. It handles url redirection, and
    // 2. Later steps depend on it.
    await initHtmlData();

    // Cache DOM elements after the HTML data is ready.
    populateElements();

    // Build or load the leaf-pane hash data after elements are populated,
    // because it may need already-cached DOM references.
    await initHashData();

    // Build the DOM trees for the custom navigation panes.
    // This is synchronous and expected to finish before later UI setup.
    buildTrees();

    // Initialize pane sizing, visibility state, and resizer behavior.
    // This is synchronous and depends on the elements already existing.
    initDualPane();

    // Start observer setup in parallel.
    // This is intentionally fire-and-forget:
    // - we do not want to block startup on it
    // - but we still catch rejections so they do not become unhandled promises
    void setupObservers().catch((err) => {
      console.error('docOnReady(): setupObservers() failed', err);
    });

    // Start search bar tweaks in parallel.
    // Same reasoning as above: do not block startup, but do handle async failure explicitly.
    void searchbarTweak().catch((err) => {
      console.error('docOnReady(): searchbarTweak() failed', err);
    });

    // Start options UI setup in parallel.
    // Again, this is intentionally not awaited, but any rejection is logged explicitly.
    void initOptions().catch((err) => {
      console.error('docOnReady(): initOptions() failed', err);
    });

    // Expose the debug/helper console object after core initialization has completed.
    // This stays synchronous and runs immediately.
    createConsoleObject();

    // Customize doxsections on pages that have it e.g. Markdown pages
    doxsectionsCustomizations();

    // Restore previous vertical scroll position for this page and save new
    // scroll positions as the user scrolls or when page hides.
    restoreAndObserveVScroll();

    // Attaches 'dp-markdown-page-nav-header-item' to page nav header items on
    // markdown pages, because Doxygen does not do that.
    markdownPagenav();
  }

  // Fires as soon as the browser has parsed the HTML and built the DOM (Document Object Model) tree — before images, 
  // stylesheets, iframes, etc., have all finished loading.
  // ⚠️ Fires much earlier than 'window.addEventListener('load', function ()...'
  // ⚠️ Guranteed that 'docOnReady()' will fire once DOM has been prased and is available 
  if (document.readyState === 'loading') {
    // The DOM is still being parsed.
    // Register a one-time callback to start initialization as soon as DOMContentLoaded fires.
    document.addEventListener('DOMContentLoaded', () => {
      // Call the async initializer without awaiting it here because this callback itself is not async.
      // Attach .catch(...) so that any rejection from docOnReady() is handled explicitly.
      void docOnReady().catch((err) => {
        console.error('docOnReady() failed', err);
      });
    });
  } else {
    // The DOM is already available, so start initialization immediately.
    // Again, attach .catch(...) so startup failures are logged and do not become unhandled rejections.
    void docOnReady().catch((err) => {
      console.error('docOnReady() failed', err);
    });
  }

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // #endregion 🟥 DOCUMENT READY CALLS

})(jQuery);